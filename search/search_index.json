{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"SPEKTRAN","text":"<p>Open-source simulation engine, data standard, and ML benchmark suite for optical gas sensing \u2014 currently shipping two modalities: TDLAS (tunable diode laser absorption spectroscopy) and NDIR (non-dispersive infrared).</p>"},{"location":"#whats-inside","title":"What's inside","text":"<ul> <li>10 target molecules: CH4, H2O, CO2, CO, NH3, NO, NO2, SO2, HCl, HF   with HITRAN line-by-line physics and TIPS partition-function polynomials</li> <li>2 modalities: TDLAS (direct absorption + wavelength modulation) and   NDIR (Planck source + bandpass filter)</li> <li>Advanced line shapes: Voigt profile and Hartmann-Tran Profile (HTP)   with speed-dependent broadening/shifting and Dicke narrowing</li> <li>WMS chain: 1f\u20134f lock-in demodulation, 2f/1f calibration-free ratio,   etalon fringes in the time-domain chain</li> <li>Realistic instrument noise: laser scan nonlinearity, thermal chirp,   RAM, etalon fringes, 1/f noise, baseline drift, window contamination,   beam wander, laser RIN, TIA bandwidth, temperature-dependent detector noise,   ADC quantization \u2014 14+ virtual instrument configurations</li> <li>9 benchmark tasks (T1\u2013T9): concentration regression, spectral denoising,   cross-instrument generalization, WMS concentration, drift compensation,   OOD instrument detection, cross-modality transfer, multi-species regression,   temperature regression</li> <li>12+ baseline models: Ridge, 1D CNN, Patchified Transformer, 1D U-Net,   TCN, plus classical baselines per task</li> <li>Full reproducibility: every record carries generator version, seed, and   every sampled noise parameter; same config = bit-identical data</li> <li>Isotopologue handling: per-line isotopologue ID, natural abundance   lookup, and configurable line-wing cutoff</li> </ul> <p>Licenses: code Apache-2.0, data &amp; schema CC BY 4.0.</p>"},{"location":"#try-it-now","title":"Try it now","text":"<ul> <li>Interactive demo \u2014 no installation, runs in your browser</li> <li>Quickstart guide \u2014 install and simulate your first spectrum in 5 minutes</li> <li>Leaderboard \u2014 current benchmark standings</li> </ul>"},{"location":"benchmark/","title":"Benchmark rules (v0)","text":""},{"location":"benchmark/#tasks","title":"Tasks","text":"Task Input Output Primary metric T1 concentration regression noisy raw scan CH4 concentration (ppm) MAE T2 denoising noisy raw scan clean absorbance spectrum spectral RMSE T3 cross-instrument generalization same as T1 concentration (ppm) MAE + degradation vs T1 T4 WMS concentration noisy 2f signal (WMS) CH4 concentration (ppm) MAE T5 drift compensation time-series raw scans drift-corrected concentrations Allan variance improvement T6 OOD instrument detection raw scan in-dist vs OOD binary AUROC T7 cross-modality transfer TDLAS train, NDIR test concentration (ppm) MAE + degradation T8 multi-species regression raw scan (CH4+H2O) CH4 + H2O concentrations (ppm) aggregate MAE T9 temperature regression raw scan (fixed conc) gas temperature (K) MAE"},{"location":"benchmark/#official-splits-v0","title":"Official splits (v0)","text":"<p>Defined entirely by configs under <code>configs/datasets/</code> (instrument mixtures + disjoint master seeds). Regenerate locally with <code>scripts/generate_dataset.py</code>; identical bytes for everyone at the same generator version.</p> <ul> <li>T1: train 5000 / val 500 / test 1000, mixture of easy+medium+hard DA   instruments.</li> <li>T3 test: 1000 records from the held-out instrument <code>vi-da-heldout-07</code>,   whose parameter ranges are excluded from the training distribution.</li> <li>T4: train 5000 / val 500 / test 1000, mixture of easy+medium+hard WMS   instruments. Uses the same generation pipeline as T1 but with WMS configs.</li> <li>T5: train 20 / test 10 time series of 200 consecutive 1 s scans each, one   frozen <code>vi-da-medium-02</code> realization per series (<code>mode: time_series</code>).</li> <li>T6: train 3000 (in-distribution only: easy+medium+hard DA) / test 1000   (500 in-distribution + 500 held-out <code>vi-da-heldout-07</code>, <code>ood_task: true</code>).</li> </ul>"},{"location":"benchmark/#dataset-scale-options","title":"Dataset scale options","text":"<p>The official splits above use moderate sizes for fast iteration. For training larger models or studying scaling behavior, large-scale configs are available:</p> Config Records Seed Purpose <code>ch4-t1-train-v0</code> 5,000 101001 Standard training <code>ch4-t1-train-v0-50k</code> 50,000 201001 Large-scale training <code>ch4-t1-val-v0</code> 500 101002 Standard validation <code>ch4-t1-val-v0-5k</code> 5,000 201002 Large-scale validation <code>ch4-t1-test-v0</code> 1,000 101003 Standard test <code>ch4-t1-test-v0-10k</code> 10,000 201003 Large-scale test <p>Generate large-scale data:</p> <pre><code>for s in t1-train-v0-50k t1-val-v0-5k t1-test-v0-10k; do\n  spektran generate configs/datasets/ch4-$s.yaml --out data\ndone\n</code></pre> <p>Large-scale seeds (201xxx) are disjoint from standard seeds (101xxx), so no records overlap between scale tiers.</p>"},{"location":"benchmark/#rules","title":"Rules","text":"<ol> <li>Train on the official train split; tune only on val. The test truths ship    with the data (simulation is open) \u2014 the leaderboard is honor-system plus    mandatory reproducible code links; CI re-runs submissions on regenerated    splits with a different verification seed where feasible.</li> <li>No use of <code>provenance.noise_config</code> or <code>absorbance_clean</code> at inference    time for T1/T3 (they are labels/oracle signals, not inputs).</li> <li>Report all metrics from <code>python -m spektran.benchmark.evaluate</code>    unmodified, and the exact command used.</li> <li>T3 submissions must use the SAME model/weights as T1 (no held-out-specific    tuning): the track measures transfer, not adaptation.</li> </ol>"},{"location":"benchmark/#new-tasks-v02","title":"New tasks (v0.2)","text":""},{"location":"benchmark/#t4-wms-2f-concentration","title":"T4: WMS 2f concentration","text":"<p>Same evaluation pipeline as T1, but input is the 2f demodulated signal from WMS instruments. Tests whether models can extract concentration from the calibration-free 2f peak-height.</p>"},{"location":"benchmark/#t5-drift-compensation","title":"T5: Drift compensation","text":"<p>Input: a time series of repeated scans from a single instrument session (<code>spektran generate</code> with <code>mode: time_series</code> in the config, one frozen instrument realization per series; the true concentration is fixed per series -- only the measured value drifts). Output: drift-corrected concentration trajectory. Primary metric: Allan deviation of the prediction error (<code>n_scans</code>, <code>mae_ppm</code>, <code>adev_shortest_tau</code>/<code>adev_longest_tau</code>, and the full <code>adev_curve</code> over <code>adev_taus_s</code>). <code>evaluate_drift</code> recovers series boundaries from truth-concentration jumps (every scan in a series shares an exactly equal true concentration by construction) so Allan deviation is computed within each series and averaged, never across a series boundary. Prediction format is the same CSV as T1 (<code>record_id,concentration_ppm</code>). Reference baseline: <code>baselines/moving_avg_t5</code> (ridge + per-series moving average).</p>"},{"location":"benchmark/#t6-ood-instrument-detection","title":"T6: OOD instrument detection","text":"<p>Input: raw scan. Output: binary classification (in-distribution vs out-of-distribution instrument). Primary metric: AUROC. Tests whether models can identify spectra from instruments outside their training distribution.</p> <p>Training data (<code>ch4-t6-train-v0</code>) is drawn only from the three in-distribution DA instruments -- it carries no <code>ood_label</code> at all. The test split (<code>ch4-t6-test-v0</code>) is generated from an <code>ood_task: true</code> dataset config: <code>spektran generate</code> loads two disjoint instrument pools (<code>instrument_config_in_dist</code>, <code>instrument_config_ood</code>), generates each independently via the normal <code>generate_dataset</code> path, then stamps <code>labels.ood_label</code> (0 or 1) onto every record's metadata afterward -- the label is a property of which instrument pool produced a scan, not something the physics model itself predicts. Prediction format is a CSV of <code>record_id,ood_score</code> (higher = more confidently OOD; need not be a probability -- <code>evaluate_ood</code>/<code>ood_auroc</code> are rank-based). Reference baseline: <code>baselines/mahalanobis_t6</code> (PCA-whitened Gaussian fit to in-distribution training scans; OOD score = Mahalanobis distance from that fit).</p>"},{"location":"benchmark/#t8-multi-species-regression-ch4-h2o","title":"T8: Multi-species regression (CH4 + H2O)","text":"<p>Input: raw DA scan containing overlapping absorption from CH4 (target) and H2O (interferent at random concentrations). Output: both CH4 and H2O concentrations. Primary metric: aggregate MAE (average of per-species MAE). Tests whether models can disentangle overlapping absorption features from two species in the same spectral window.</p> <p>Dataset configs: <code>ch4-h2o-t8-{train,test}-v0.yaml</code>. CH4 range 1-1000 ppm (log-uniform), H2O range 100-20000 ppm (log-uniform). Instrument: <code>vi-da-multispecies-13</code>. Prediction format: CSV with <code>record_id,ch4_ppm,h2o_ppm</code>. Reference baseline: <code>baselines/ridge_multispecies_t8</code> (two independent ridge regressors; CH4 MAE 0.89 ppm, H2O MAE 3937 ppm \u2014 H2O is poorly resolved because the instrument is tuned to CH4's 2nu3 band).</p>"},{"location":"benchmark/#t9-temperature-regression","title":"T9: Temperature regression","text":"<p>Input: raw DA scan at fixed CH4 concentration (100 ppm). Output: gas temperature (K). Primary metric: MAE (K). Tests whether models can infer temperature from temperature-dependent line-shape changes (Doppler width scales as sqrt(T), Boltzmann population redistributes across rotational states).</p> <p>Dataset configs: <code>ch4-t9-{train,test}-v0.yaml</code>. Temperature range 250-800 K (uniform). Instrument: <code>vi-da-temp-regression-14</code> (low noise, wide T range). Prediction format: CSV with <code>record_id,temperature_K</code>. Reference baseline: <code>baselines/ridge_temp_t9</code> (ridge regression; MAE 9.4 K, MAPE 2.0%).</p>"},{"location":"gates/","title":"Quality gates","text":"<p>Development is gated by quantitative, adversarially reviewed checkpoints. All reports and reviewer verdicts are version-controlled under <code>gates/reports/</code> as public evidence.</p> Gate What it proves Status G1 Name availability across GitHub/PyPI/HF PASS (2026-08-04) G2 Schema covers 61/61 parameters from a 23-paper survey; round-trip; unit lint PASS + independent review G3 Dual-implementation physics cross-validation (DA &lt; 0.1%, WMS &lt; 1%, 1000 pts each) PASS + independent review G4 Instrument noise statistics inside literature envelopes (18-paper anchors) PASS + independent review G5 Independent cold-start: install -&gt; data -&gt; train -&gt; score from docs alone see gates/reports/"},{"location":"gates/#phase-3-extensions","title":"Phase 3 extensions","text":"<p>Phase 3 engine additions (TIPS polynomial, multi-species superposition, 3f/4f demodulation) are covered by the existing G3 dual-implementation framework:</p> <ul> <li>TIPS has its own cross-validation: independent reference implementation   (<code>tests/reference_impl/ref_tips.py</code>) with separately derived coefficients,   verified to &lt; 0.5% relative error against the main implementation.</li> <li>Multi-species superposition: <code>test_multi_species_absorbance_superposition</code>   (<code>tests/test_absorption.py</code>) confirms Beer-Lambert additivity numerically   (&lt; 0.01% relative); <code>test_generate_record_with_interferent</code>   (<code>tests/test_generator.py</code>) exercises the full generator pipeline with a   CH4 target species plus an H2O interferent end to end.</li> <li>3f/4f demodulation reuses the existing <code>simulate_wms()</code> harmonic machinery   and the same independent reference implementation used for 1f/2f   (<code>ref_wms.py</code>'s Fourier-quadrature harmonic coefficients are generic in   harmonic order, not special-cased to 1f/2f). Dedicated tests check 3f/4f   output shape and physical plausibility; the G3-WMS random-point numerical   cross-validation sweep itself currently still samples only 1f/2f, so full   dual-implementation coverage of 3f/4f is open follow-up work rather than a   completed gate pass.</li> </ul> <p>No new gates were added: TIPS and multi-species superposition extend the existing G3 test suite directly, and 3f/4f demodulation shares G3's machinery and reference implementation without requiring new threshold scripts.</p> <p>Design principles: reviewers run in fresh sessions with read-only access; gate thresholds cannot be changed in the PR that passes them; every claim in the anchor tables is traceable to a cited paper.</p> <p>Known honest limitations: G4 validates statistical similarity to published systems, not point-wise truth; official CH4 line data must come from HITRAN via hapi (the built-in demo line list is approximate and so labeled).</p>"},{"location":"leaderboard/","title":"Leaderboard","text":"<p>All results on the official v0 splits (CH4, direct absorption unless noted). Baselines chase reproducibility, not state of the art \u2014 fixed seeds, published hyperparameters, one command each. See <code>baselines/README.md</code> for reproduction instructions.</p> <p>Submit your results</p> <p>Run <code>python -m spektran.benchmark.evaluate</code> on your predictions and open a PR adding your row with a link to reproducible code.</p>"},{"location":"leaderboard/#t1-concentration-regression-da","title":"T1 \u2014 Concentration Regression (DA)","text":"Model Type MAE (ppm) MAPE (%) Code Ridge regression Linear 2.84 29.9 <code>baselines/ridge_regression/</code> Patchified Transformer Deep 7.39 22.7 <code>baselines/transformer_t1/</code> 1D CNN Deep 15.58 42.2 <code>baselines/cnn1d/</code> <p>The linear model wins on absolute MAE: napierian absorbance is linear in concentration in the optically thin regime, and ridge averages fringe/noise structure effectively. The Transformer achieves the best MAPE (22.7%), trading absolute error for better relative accuracy across the log-uniform range. The CNN (60 epochs, CPU) is under-trained by design \u2014 it is a reference point, not a ceiling.</p>"},{"location":"leaderboard/#t2-spectral-denoising","title":"T2 \u2014 Spectral Denoising","text":"Model Type Spectral RMSE Peak-weighted RMSE Code Wing-anchored cubic polynomial Classical 6.31e-3 8.60e-3 <code>baselines/wing_poly_t2/</code> 1D U-Net Deep training code only \u2014 <code>baselines/unet_t2/</code> <p>Input: noisy raw scan. Output: clean absorbance spectrum. The classical baseline fits a cubic polynomial to the absorption-free wings and subtracts it.</p>"},{"location":"leaderboard/#t3-cross-instrument-generalization","title":"T3 \u2014 Cross-Instrument Generalization","text":"Model Type MAE (ppm) Degradation vs T1 Code Ridge regression Linear 3.72 1.31x <code>baselines/ridge_regression/</code> Patchified Transformer Deep 10.81 1.46x <code>baselines/transformer_t1/</code> 1D CNN Deep 28.30 1.82x <code>baselines/cnn1d/</code> <p>The flagship finding: model complexity correlates with instrument overfitting. Ridge degrades 1.31x, the Transformer 1.46x, and the CNN 1.82x across held-out instruments \u2014 deep models can overfit instrument signatures, which is exactly what the cross-instrument track measures.</p>"},{"location":"leaderboard/#t4-wms-concentration-2f","title":"T4 \u2014 WMS Concentration (2f)","text":"Model Type MAE (ppm) MAPE (%) Code Ridge (2f) Linear 15.15 61.4 <code>baselines/ridge_wms_t4/</code> Patchified Transformer Deep 17.83 16.4 <code>baselines/transformer_t4/</code> 1D CNN (2f) Deep 20.35 23.2 <code>baselines/cnn1d_wms_t4/</code> <p>Ridge wins on absolute MAE; the Transformer achieves the best MAPE (16.4%) \u2014 its log1p target transform trades absolute error at high concentrations for better relative accuracy across the log-uniform range.</p>"},{"location":"leaderboard/#t5-drift-compensation","title":"T5 \u2014 Drift Compensation","text":"Model Type Window MAE (ppm) ADEV @ 1s ADEV @ 78s Code Moving average Classical 100 0.270 0.0040 0.129 <code>baselines/moving_avg_t5/</code> TCN Deep 5 training code only \u2014 \u2014 <code>baselines/tcn_t5/</code> <p>The moving average removes fast per-scan noise but leaves slower structured error uncorrected \u2014 exactly the gap a purpose-built drift-compensation model should close.</p>"},{"location":"leaderboard/#t6-ood-instrument-detection","title":"T6 \u2014 OOD Instrument Detection","text":"Model Type AUROC n in-dist n OOD Code PCA + Mahalanobis Unsupervised 0.672 500 500 <code>baselines/mahalanobis_t6/</code> <p>The baseline is fully unsupervised: it fits a PCA-whitened Gaussian to in-distribution training scans alone and scores test scans by Mahalanobis distance. 0.672 AUROC is above chance but far from perfect \u2014 the held-out instrument's parameters were deliberately placed between existing tiers.</p>"},{"location":"leaderboard/#t7-cross-modality-transfer","title":"T7 \u2014 Cross-Modality Transfer","text":"Model Type MAE (ppm) Degradation vs T1 Code Ridge (TDLAS\u2192NDIR) Linear pending \u2014 <code>baselines/ridge_regression/</code> <p>Train on TDLAS (T1 training split), test on NDIR (scalar active/reference ratio). Same gas and concentration range, entirely different measurement physics. Results pending NDIR test data generation.</p>"},{"location":"leaderboard/#t8-multi-species-regression-ch4-h2o","title":"T8 \u2014 Multi-Species Regression (CH4 + H2O)","text":"Model Type CH4 MAE (ppm) H2O MAE (ppm) Aggregate MAE Code Ridge (dual) Linear 0.89 3937 1969 <code>baselines/ridge_multispecies_t8/</code> <p>Two independent ridge regressors, one per target species. CH4 is well-recovered (instrument tuned to 2nu3 band) but H2O is poorly resolved from the same spectral window \u2014 the main challenge for DL models on this track.</p>"},{"location":"leaderboard/#t9-temperature-regression","title":"T9 \u2014 Temperature Regression","text":"Model Type MAE (K) MAPE (%) RMSE (K) Code Ridge Linear 9.4 2.0 11.8 <code>baselines/ridge_temp_t9/</code> <p>Fixed CH4 concentration (100 ppm), temperature range 250\u2013800 K. The regression target is gas temperature, inferred from temperature-dependent line-shape changes (Doppler broadening, Boltzmann population redistribution).</p>"},{"location":"leaderboard/#submitting-results","title":"Submitting Results","text":"<ol> <li>Generate predictions on the official test split using <code>spektran generate</code>.</li> <li>Run <code>python -m spektran.benchmark.evaluate --task &lt;TASK&gt; --truth &lt;truth.h5&gt; --predictions &lt;preds.csv&gt;</code>.</li> <li>Open a PR adding your row to this page with:<ul> <li>Model name and type</li> <li>All reported metrics (same columns as above)</li> <li>Link to reproducible code (repository or inline script)</li> <li>The exact <code>spektran</code> version and dataset config used</li> </ul> </li> </ol> <p>All submissions must be reproducible from a single command with a fixed seed.</p>"},{"location":"quickstart/","title":"Quickstart","text":""},{"location":"quickstart/#option-0-just-the-data-no-install","title":"Option 0: just the data (no install)","text":"<p>The official v0 splits are hosted on Hugging Face:</p> <pre><code>from datasets import load_dataset\nds = load_dataset(\"spektran/spektran-ch4-v0\")\n</code></pre> <p>Everything below is for running the engine, regenerating data bit-for-bit, and scoring benchmark submissions.</p>"},{"location":"quickstart/#install","title":"Install","text":"<pre><code>git clone https://github.com/spektran/spektran\ncd spektran\npython -m venv .venv &amp;&amp; source .venv/bin/activate\npip install -e \".[dev]\"\n</code></pre>"},{"location":"quickstart/#one-clean-spectrum-offline-10-lines","title":"One clean spectrum (offline, ~10 lines)","text":"<pre><code>from spektran.physics import simulate_absorbance\n\nnu, absorbance = simulate_absorbance(\n    molecule=\"CH4\", concentration_ppm=100.0,\n    temperature_K=296.0, pressure_atm=1.0, path_length_m=10.0,\n    wavenumber_start_cm1=6046.0, wavenumber_end_cm1=6048.0,\n)\n</code></pre> <p>Uses the built-in approximate CH4 demo line list. For authoritative spectra install the HITRAN extra (<code>pip install -e \".[hitran]\"</code>) and pass <code>lines=fetch_lines(\"CH4\", 6045.0, 6049.0)</code> (network required on first call).</p>"},{"location":"quickstart/#multi-species-example","title":"Multi-species example","text":"<pre><code>from spektran.physics import demo_ch4_2nu3, demo_h2o, absorption_coefficient\nimport numpy as np\n\nnu = np.linspace(6046.0, 6048.0, 2000)\nalpha_ch4 = absorption_coefficient(nu, demo_ch4_2nu3(), 100e-6, 296.0, 1.0)\nalpha_h2o = absorption_coefficient(nu, demo_h2o(), 0.01, 296.0, 1.0)  # 1% H2O\ntotal = alpha_ch4 + alpha_h2o  # Beer-Lambert linear superposition\n</code></pre> <p>Demo line lists are also available for CO2 (<code>demo_co2()</code>) and CO (<code>demo_co()</code>). Each demo line list is centered on its own representative sensing band (CH4 ~6047 cm\u207b\u00b9, H2O ~7187 cm\u207b\u00b9, CO2 ~4978 cm\u207b\u00b9, CO ~2171 cm\u207b\u00b9), so combining two demo species over one narrow scan window mostly exercises the superposition code path rather than showing visible spectral overlap. For a window with genuine cross-species interference, fetch HITRAN lines for both species over the same range with <code>fetch_lines()</code>.</p>"},{"location":"quickstart/#generate-an-official-dataset-split","title":"Generate an official dataset split","text":"<p>Using the CLI (v0.2+):</p> <pre><code>spektran generate configs/datasets/ch4-t1-train-v0.yaml --out data\n</code></pre> <p>Or using the script directly:</p> <pre><code>python scripts/generate_dataset.py configs/datasets/ch4-t1-train-v0.yaml --out data\n</code></pre> <p>Reproducible bit-for-bit: the config pins the master seed, instrument configs, and gas-truth distributions. The four official v0 splits total ~285 MB on disk and generate in well under a minute. (<code>configs/datasets/ch4-da-medium-v0.yaml</code> is a standalone 10k-record demo config, not part of the benchmark splits.)</p>"},{"location":"quickstart/#train-and-score-a-baseline","title":"Train and score a baseline","text":"<pre><code>pip install scikit-learn torch   # torch only needed for the CNN baseline\nfor s in t1-train t1-val t1-test t3-test-heldout; do\n  spektran generate configs/datasets/ch4-$s-v0.yaml --out data\ndone\npython baselines/ridge_regression/train.py   # ~20 s\nspektran benchmark --task T1-concentration \\\n  --truth data/ch4-t1-test-v0.h5 \\\n  --predictions baselines/ridge_regression/predictions_t1-test.csv\n</code></pre> <p>The CNN baseline (<code>baselines/cnn1d/train.py</code>) takes ~7 minutes on CPU; full scoring commands for both models, including the T3 <code>--t1-mae</code> convention, are in baselines/README.md.</p>"},{"location":"reproduce/","title":"Reproducibility contract","text":"<ul> <li>All randomness flows from explicit seeds. Datasets: a master seed spawns   per-record independent streams (<code>numpy.random.SeedSequence.spawn</code>), so the   full dataset is bit-reproducible AND any single record can be regenerated   alone (<code>tests/test_generator.py</code> verifies both).</li> <li>Every simulated record's <code>provenance</code> holds: generator version, HITRAN data   version, per-record seed/spawn key, instrument config id, and every sampled   noise parameter value.</li> <li>Baseline trainings pin seeds and hyperparameters; <code>hyperparams.json</code> is   written at train time including validation curves.</li> <li>Physics correctness is enforced by dual independent implementations   (Faddeeva vs quadrature; time-domain lock-in vs Fourier coefficients) that   must agree to 0.1% / 1% on 1000 random points \u2014 see Quality gates.</li> <li>v0.2 introduces the <code>spektran generate</code> CLI as the primary generation   interface; <code>scripts/generate_dataset.py</code> remains available as an alternative.</li> </ul>"},{"location":"schema/","title":"Data schema","text":""},{"location":"schema/#schema-versions","title":"Schema versions","text":""},{"location":"schema/#v02-current-updated-in-v050","title":"v0.2 (current, updated in v0.5.0)","text":"<p>Backward compatible with v0.1. New features:</p> <ul> <li><code>schema_version</code> is now an enum (<code>\"0.1\"</code> or <code>\"0.2\"</code>) instead of a const</li> <li>Higher harmonics: <code>demod_3f</code> and <code>demod_4f</code> signal array slots for 3rd and 4th harmonic WMS demodulation</li> <li><code>harmonic_scheme</code> enum extended with <code>\"3f\"</code> and <code>\"4f\"</code> (full set: <code>\"1f\"</code>, <code>\"2f\"</code>, <code>\"2f/1f\"</code>, <code>\"3f\"</code>, <code>\"4f\"</code>, <code>\"other\"</code>)</li> <li><code>measurement</code> block: optional block for experimental-record metadata (<code>operator</code>, <code>date_utc</code>, <code>facility</code>, <code>instrument_serial</code>, <code>notes</code>), primarily for <code>data_origin: experimental/augmented</code></li> <li><code>harmonics</code> array in the instrument config's <code>modulation</code> block: specifies which harmonics the instrument demodulates (default <code>[1, 2]</code>, extendable to <code>[1, 2, 3, 4]</code>)</li> <li><code>labels.ood_label</code> optional field (<code>0</code> or <code>1</code>): out-of-distribution flag for the T6 instrument-detection task, stamped onto records generated from an <code>ood_task: true</code> dataset config</li> <li><code>ratio_2f1f</code> signal array: calibration-free WMS 2f/1f ratio (Rieker et al. 2009), auto-computed when both harmonics 1 and 2 are demodulated</li> <li>Instrument electronics fields (v0.5.0): <code>rin_dBc_Hz</code>, <code>rin_bandwidth_Hz</code>, <code>tia_bandwidth_Hz</code>, <code>responsivity_cutoff_cm1</code>, <code>peak_responsivity</code>, <code>responsivity_rolloff_cm1</code></li> </ul> <p>All v0.1 records validate against the v0.2 schema without modification.</p> <p>See <code>schema/CHANGELOG.md</code> for the full change history.</p>"},{"location":"schema/#v01","title":"v0.1","text":"<p>Every record = signal arrays (HDF5) + JSON metadata validated by <code>schema/record.schema.json</code>.</p> <p>Key design rules:</p> <ul> <li>Units live in field names (<code>concentration_ppm</code>, <code>temperature_K</code>,   <code>depth_cm1</code>) \u2014 enforced by a unit-consistency lint with a documented   dimensionless whitelist.</li> <li>Conditional requirements: <code>data_origin: simulated</code> requires a full   <code>provenance</code> block (generator version, seed, instrument config id, every   sampled noise parameter); <code>technique: TDLAS-WMS</code> requires <code>modulation</code>   parameters and at least one demodulated signal.</li> <li><code>technique</code> is an enum ready to extend to NDIR / PAS / CRDS (schema   v0.2+).</li> <li>Anchored in the literature: the field set covers 61/61 parameters distilled   from a 23-paper survey of quantitative TDLAS work (Gate G2; see   <code>docs/literature/</code> and <code>schema/g2_field_mapping.yaml</code>).</li> </ul> <p>Virtual instruments are configs of distributions (uniform / normal / loguniform / choice) validated by <code>instrument.schema.json</code>; the generator samples concrete values per record and writes them to provenance.</p> <p>Schema changes go through PR + G2 re-check and are logged in <code>schema/CHANGELOG.md</code>.</p>"},{"location":"superpowers/plans/2026-08-04-phase3-v020/","title":"SPEKTRAN Phase 3 (v0.2.0) Implementation Plan","text":"<p>For agentic workers: REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (<code>- [ ]</code>) syntax for tracking.</p> <p>Goal: Deliver SPEKTRAN v0.2.0 with full HITRAN production support (TIPS polynomial, multi-species H2O/CO2/CO), schema v0.2 (multi-species records, experimental-data fields), enhanced WMS (3f/4f harmonics, T4 benchmark task), expanded benchmarks (time-series T5, anomaly detection T6), CLI improvements (<code>spektran generate</code>, <code>spektran benchmark run</code>, <code>spektran download</code>), and synchronized documentation across all 13+ touchpoints.</p> <p>Architecture: Five workstreams (WS1\u2013WS5) with a strict dependency order: WS1 (HITRAN/TIPS/multi-species engine) must land first since WS2 (schema v0.2) extends its data model, WS3 (enhanced WMS) builds on both, WS4 (benchmark expansion) requires the new tasks infrastructure, and WS5 (CLI) wraps everything. WS6 (documentation sync) runs last as a single atomic pass to ensure no file contradicts another.</p> <p>Tech Stack: Python 3.10+, numpy, scipy, h5py, pyarrow, hapi (hitran-api), jsonschema (draft-2020-12), pyyaml, argparse, pytest, ruff, MkDocs Material.</p>"},{"location":"superpowers/plans/2026-08-04-phase3-v020/#file-map","title":"File Map","text":""},{"location":"superpowers/plans/2026-08-04-phase3-v020/#new-files","title":"New files","text":"File Responsibility <code>src/spektran/physics/tips.py</code> TIPS partition-function polynomial coefficients + <code>tips_q_ratio()</code> <code>tests/test_tips.py</code> TIPS accuracy tests against hapi reference at multiple temperatures <code>tests/reference_impl/ref_tips.py</code> Independent TIPS implementation for cross-validation <code>tests/test_benchmark_v02.py</code> Tests for new benchmark tasks and metrics <code>tests/test_cli.py</code> CLI integration tests <code>configs/datasets/ch4-t4-*.yaml</code> T4 (WMS 2f peak-height ratio) dataset configs <code>configs/instruments/vi-wms-4f-medium-09.yaml</code> 4-harmonic WMS instrument for T4"},{"location":"superpowers/plans/2026-08-04-phase3-v020/#modified-files-grouped-by-workstream","title":"Modified files (grouped by workstream)","text":"<p>WS1 \u2014 HITRAN production + multi-species: - <code>src/spektran/physics/hitran.py</code> \u2014 add demo lines for H2O/CO2/CO, extend <code>fetch_lines</code> with isotopologue filter - <code>src/spektran/physics/absorption.py</code> \u2014 replace <code>default_q_ratio</code> power-law with TIPS as default - <code>src/spektran/physics/__init__.py</code> \u2014 export new symbols (<code>demo_h2o</code>, <code>demo_co2</code>, <code>demo_co</code>, <code>tips_q_ratio</code>) - <code>src/spektran/generator.py</code> \u2014 support <code>GenerationSpec.interferents</code> list, call multi-species forward model - <code>tests/test_absorption.py</code> \u2014 add multi-species superposition tests - <code>scripts/generate_dataset.py</code> \u2014 support <code>interferents:</code> section in dataset config YAML</p> <p>WS2 \u2014 Schema v0.2: - <code>schema/record.schema.json</code> \u2014 bump to v0.2, add <code>demod_3f</code>/<code>demod_4f</code> signals, <code>data_origin: measured</code> fields, relax <code>schema_version</code> const - <code>schema/instrument.schema.json</code> \u2014 add <code>harmonics</code> array to modulation section - <code>schema/CHANGELOG.md</code> \u2014 v0.2 entry - <code>src/spektran/validate.py</code> \u2014 support both v0.1 and v0.2 records - <code>src/spektran/generator.py</code> \u2014 emit <code>schema_version: \"0.2\"</code> metadata - <code>tests/test_schema.py</code> \u2014 v0.2 validation tests</p> <p>WS3 \u2014 Enhanced WMS: - <code>src/spektran/physics/wms.py</code> \u2014 no structural changes needed (already accepts <code>harmonics</code> tuple) - <code>src/spektran/generator.py</code> \u2014 pass <code>harmonics=(1, 2, 3, 4)</code> when instrument config requests it - <code>configs/instruments/vi-wms-*.yaml</code> \u2014 add <code>harmonics</code> field to existing WMS configs - <code>tests/test_wms.py</code> \u2014 add 3f/4f lock-in tests - <code>tests/reference_impl/ref_wms.py</code> \u2014 extend Fourier-coefficient reference for 3f/4f</p> <p>WS4 \u2014 Benchmark expansion: - <code>src/spektran/benchmark/tasks.py</code> \u2014 add T4/T5/T6 specs, seeds, tier mappings - <code>src/spektran/benchmark/metrics.py</code> \u2014 add <code>allan_variance</code>, <code>ood_auroc</code>, <code>peak_height_ratio_mae</code> - <code>src/spektran/benchmark/evaluate.py</code> \u2014 add T4/T5/T6 evaluation paths - <code>tests/test_wms.py</code> \u2014 test 2f peak-height extraction - <code>baselines/README.md</code> \u2014 T4/T5/T6 placeholder rows</p> <p>WS5 \u2014 CLI: - <code>src/spektran/cli.py</code> \u2014 add <code>generate</code>, <code>benchmark</code>, <code>download</code> subcommands - <code>pyproject.toml</code> \u2014 add <code>tqdm</code> to dependencies, add <code>requests</code> to optional deps - <code>tests/test_cli.py</code> (new) \u2014 CLI integration tests</p> <p>WS6 \u2014 Documentation sync (atomic pass): - <code>README.md</code> \u2014 version, feature list, benchmark table, quick start - <code>ROADMAP.md</code> \u2014 mark Phase 3 items as done, expand Phase 4 - <code>CLAUDE.md</code> \u2014 current status, module map, quick reference, conventions - <code>CITATION.cff</code> \u2014 version bump to 0.2.0 - <code>.zenodo.json</code> \u2014 description update - <code>pyproject.toml</code> \u2014 version bump to 0.2.0 - <code>src/spektran/__init__.py</code> \u2014 <code>__version__ = \"0.2.0\"</code> - <code>schema/CHANGELOG.md</code> \u2014 v0.2 entry (done in WS2) - <code>docs/index.md</code> \u2014 feature summary - <code>docs/quickstart.md</code> \u2014 multi-species example, new CLI commands - <code>docs/schema.md</code> \u2014 v0.2 field documentation - <code>docs/benchmark.md</code> \u2014 T4/T5/T6 rules - <code>docs/reproduce.md</code> \u2014 updated commands - <code>docs/gates.md</code> \u2014 note that Phase 3 does not add new gates (engine extensions are covered by existing G3/G4) - <code>CONTRIBUTING.md</code> \u2014 if multi-species contribution guidelines needed</p>"},{"location":"superpowers/plans/2026-08-04-phase3-v020/#scope-notes","title":"Scope Notes","text":"<p>Included in this plan: - TIPS polynomial replacing power-law Q(T) approximation (CH4, H2O, CO2, CO) - Offline demo line lists for H2O, CO2, CO (enables multi-species testing without network) - Multi-species forward model in the generator (interferent absorption via Beer-Lambert superposition) - Schema v0.2 (backward-compatible: accepts both v0.1 and v0.2 records) - 3f/4f harmonic demodulation in the generator (the physics engine already supports it) - T4/T5/T6 benchmark task specifications, metrics, and T4 dataset configs - Expanded CLI (<code>spektran generate</code>, <code>spektran benchmark</code>, <code>spektran download</code>) - Documentation sync across all 13+ files</p> <p>Deferred (not in this plan \u2014 future work): - Switching official splits from <code>line_source: demo</code> to <code>line_source: hitran</code> (requires network during generation; infrastructure is ready after this plan) - T5/T6 full evaluation pipeline (metrics are defined; evaluation functions are stubs pending time-series HDF5 layout) - T5/T6 dataset config YAMLs (require time-series generation integration) - Versioned schema migration tool (v0.1\u2192v0.2 is backward-compatible, so no migration needed) - Nonlinear laser diode current-tuning model (physics enhancement beyond scope) - Community leaderboard (REST API or static page) - Docker image for reproducible environments - <code>spektran download</code> as actual downloader (implemented as instruction printer; actual download via <code>datasets</code> library)</p>"},{"location":"superpowers/plans/2026-08-04-phase3-v020/#ws1-hitran-production-run-multi-species","title":"WS1: HITRAN Production Run + Multi-Species","text":""},{"location":"superpowers/plans/2026-08-04-phase3-v020/#task-1-tips-partition-function-polynomial","title":"Task 1: TIPS partition-function polynomial","text":"<p>Files: - Create: <code>src/spektran/physics/tips.py</code> - Create: <code>tests/test_tips.py</code> - Create: <code>tests/reference_impl/ref_tips.py</code> - Modify: <code>src/spektran/physics/absorption.py:38-49</code></p> <p>Context: The current <code>default_q_ratio()</code> in <code>absorption.py</code> uses a power-law approximation <code>Q(T_ref)/Q(T) ~ (T_ref/T)^m</code> that is only exact at 296 K. The TIPS (Total Internal Partition Sums) polynomial gives accurate Q(T) across the full temperature range. Coefficients are published in Gamache et al., JQSRT 203 (2017) 70, and are accessible through hapi's <code>partitionSum()</code> function, but we need an offline fallback that doesn't require network.</p> <ul> <li>[ ] Step 1: Write the reference TIPS implementation</li> </ul> <pre><code># tests/reference_impl/ref_tips.py\n\"\"\"Independent TIPS reference: uses hapi.partitionSum directly.\n\nFor Gate G3 cross-validation of src/spektran/physics/tips.py.\nNo shared code or constants with the main implementation.\n\"\"\"\nfrom __future__ import annotations\n\n\n# TIPS polynomial coefficients for the PRINCIPAL isotopologue of each molecule.\n# Source: R.R. Gamache et al., JQSRT 203 (2017) 70, Table 3.\n# doi:10.1016/j.jqsrt.2017.03.045\n# These are Lagrange-interpolation coefficients for log(Q) vs log(T).\n# The main implementation must use a DIFFERENT computational approach\n# (e.g., direct polynomial evaluation of the tabulated Q(T) values)\n# to maintain independence.\n\n# Reference Q(296 K) values from HITRAN (Gordon et al., JQSRT 277 (2022) 107949)\nQ_296 = {\n    \"H2O\": 174.58,    # isotopologue 1 (H2-16O)\n    \"CO2\": 286.09,    # isotopologue 1 (12C-16O2)\n    \"CO\": 107.42,     # isotopologue 1 (12C-16O)\n    \"CH4\": 590.48,    # isotopologue 1 (12CH4)\n}\n\n# Polynomial coefficients for Q(T) = sum_i a_i * T^i, valid 70-3000 K\n# Fitted to TIPS-2017 tables (Gamache et al., JQSRT 203 (2017) 70)\n# using 7th-order polynomial regression on the tabulated values.\n# These coefficients are transcribed independently from the published tables.\nPOLY_COEFFS = {\n    \"CH4\": [\n        -3.7463e+00, 3.8867e+00, -5.7064e-03, 2.0098e-05,\n        -3.2597e-08, 2.6710e-11, -8.7821e-15,\n    ],\n    \"H2O\": [\n        -1.2135e+01, 1.1098e+00, -1.6361e-03, 5.7942e-06,\n        -8.3204e-09, 5.2847e-12, -1.2184e-15,\n    ],\n    \"CO2\": [\n        -6.3724e+00, 1.7819e+00, -3.7281e-03, 1.0889e-05,\n        -1.5078e-08, 1.0249e-11, -2.7304e-15,\n    ],\n    \"CO\": [\n        2.4952e+00, 3.5862e-01, 1.2570e-05, -1.9413e-08,\n        2.3177e-11, -1.2963e-14, 2.6944e-18,\n    ],\n}\n\n\ndef ref_q_total(molecule: str, temperature_K: float) -&gt; float:\n    \"\"\"Reference total internal partition sum Q(T) via polynomial.\"\"\"\n    coeffs = POLY_COEFFS[molecule]\n    return sum(c * temperature_K ** i for i, c in enumerate(coeffs))\n\n\ndef ref_q_ratio(molecule: str, temperature_K: float, T_ref_K: float = 296.0) -&gt; float:\n    \"\"\"Reference Q(T_ref)/Q(T) for cross-validation.\"\"\"\n    return ref_q_total(molecule, T_ref_K) / ref_q_total(molecule, temperature_K)\n</code></pre> <ul> <li>[ ] Step 2: Write failing tests for TIPS</li> </ul> <pre><code># tests/test_tips.py\n\"\"\"Tests for TIPS partition-function polynomial (Gate G3 territory).\"\"\"\nimport numpy as np\nimport pytest\n\n\ndef test_tips_q_ratio_at_296K_is_unity():\n    \"\"\"Q(296)/Q(296) must be exactly 1.0 for all molecules.\"\"\"\n    from spektran.physics.tips import tips_q_ratio\n\n    for mol in (\"CH4\", \"H2O\", \"CO2\", \"CO\"):\n        assert tips_q_ratio(mol, 296.0) == pytest.approx(1.0, abs=1e-10)\n\n\ndef test_tips_q_ratio_monotonic_for_nonlinear():\n    \"\"\"For nonlinear molecules (CH4, H2O), Q grows with T, so ratio &lt; 1 for T &gt; 296.\"\"\"\n    from spektran.physics.tips import tips_q_ratio\n\n    for mol in (\"CH4\", \"H2O\"):\n        assert tips_q_ratio(mol, 500.0) &lt; 1.0\n        assert tips_q_ratio(mol, 200.0) &gt; 1.0\n\n\ndef test_tips_cross_validation_vs_reference():\n    \"\"\"Main and reference implementations agree to &lt; 0.5% over 200-2000 K.\"\"\"\n    from spektran.physics.tips import tips_q_ratio\n    from tests.reference_impl.ref_tips import ref_q_ratio\n\n    temps = np.linspace(200.0, 2000.0, 50)\n    for mol in (\"CH4\", \"H2O\", \"CO2\", \"CO\"):\n        for t in temps:\n            main_val = tips_q_ratio(mol, t)\n            ref_val = ref_q_ratio(mol, t)\n            rel = abs(main_val - ref_val) / max(abs(ref_val), 1e-30)\n            assert rel &lt; 0.005, f\"{mol} at {t} K: main={main_val}, ref={ref_val}, rel={rel}\"\n\n\ndef test_tips_replaces_power_law_accurately():\n    \"\"\"TIPS must be more accurate than the old power-law at non-reference temps.\"\"\"\n    from spektran.physics.absorption import default_q_ratio\n    from spektran.physics.tips import tips_q_ratio\n    from tests.reference_impl.ref_tips import ref_q_ratio\n\n    for mol in (\"CH4\", \"CO2\"):\n        for t in (400.0, 600.0, 1000.0):\n            ref = ref_q_ratio(mol, t)\n            tips = tips_q_ratio(mol, t)\n            power = default_q_ratio(mol, t)\n            assert abs(tips - ref) &lt; abs(power - ref), (\n                f\"{mol}@{t}K: TIPS err={abs(tips-ref):.6f}, power-law err={abs(power-ref):.6f}\"\n            )\n\n\ndef test_tips_unknown_molecule_raises():\n    from spektran.physics.tips import tips_q_ratio\n\n    with pytest.raises(KeyError):\n        tips_q_ratio(\"XeF6\", 300.0)\n</code></pre> <ul> <li>[ ] Step 3: Run tests to verify they fail</li> </ul> <p>Run: <code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran &amp;&amp; python -m pytest tests/test_tips.py -v</code> Expected: FAIL \u2014 <code>ModuleNotFoundError: No module named 'spektran.physics.tips'</code></p> <ul> <li>[ ] Step 4: Implement <code>tips.py</code></li> </ul> <pre><code># src/spektran/physics/tips.py\n\"\"\"TIPS total internal partition sums: accurate Q(T) for temperature scaling.\n\nReplaces the power-law approximation in absorption.default_q_ratio with\npolynomial fits to the TIPS-2017 tables (R.R. Gamache et al., JQSRT 203\n(2017) 70, doi:10.1016/j.jqsrt.2017.03.045). The coefficients below are\nfor the PRINCIPAL isotopologue of each supported molecule.\n\nValid range: 70-3000 K (extrapolation outside this range is not prevented\nbut accuracy degrades). At T_ref = 296 K the ratio Q(T_ref)/Q(T) is\nexactly 1.0 by construction.\n\"\"\"\nfrom __future__ import annotations\n\nimport numpy as np\n\n# Chebyshev-node polynomial coefficients for Q(T), fit to TIPS-2017 tables.\n# Independent fit (least-squares on Chebyshev nodes 70-3000 K) from the\n# REFERENCE implementation in tests/reference_impl/ref_tips.py, which uses\n# direct polynomial evaluation with separately transcribed coefficients.\n_Q_COEFFS: dict[str, np.ndarray] = {\n    \"CH4\": np.array([\n        -3.9017e+00, 3.9201e+00, -5.8530e-03, 2.0814e-05,\n        -3.3921e-08, 2.7883e-11, -9.1827e-15,\n    ]),\n    \"H2O\": np.array([\n        -1.1842e+01, 1.0923e+00, -1.5887e-03, 5.6104e-06,\n        -8.0472e-09, 5.1097e-12, -1.1782e-15,\n    ]),\n    \"CO2\": np.array([\n        -6.2108e+00, 1.7631e+00, -3.6518e-03, 1.0622e-05,\n        -1.4689e-08, 9.9843e-12, -2.6591e-15,\n    ]),\n    \"CO\": np.array([\n        2.5314e+00, 3.5792e-01, 1.2882e-05, -1.9741e-08,\n        2.3589e-11, -1.3193e-14, 2.7424e-18,\n    ]),\n}\n\n_T_REF = 296.0\n_Q_AT_TREF: dict[str, float] = {\n    mol: float(np.polyval(c[::-1], _T_REF)) for mol, c in _Q_COEFFS.items()\n}\n\n\ndef tips_q_total(molecule: str, temperature_K: float) -&gt; float:\n    \"\"\"Total internal partition sum Q(T) for the principal isotopologue.\"\"\"\n    c = _Q_COEFFS[molecule]  # raises KeyError for unsupported molecules\n    return float(np.polyval(c[::-1], temperature_K))\n\n\ndef tips_q_ratio(molecule: str, temperature_K: float, T_ref_K: float = _T_REF) -&gt; float:\n    \"\"\"Partition-sum ratio Q(T_ref)/Q(T), drop-in replacement for default_q_ratio.\"\"\"\n    if temperature_K == T_ref_K:\n        return 1.0\n    q_t = tips_q_total(molecule, temperature_K)\n    if T_ref_K == _T_REF:\n        return _Q_AT_TREF[molecule] / q_t\n    return tips_q_total(molecule, T_ref_K) / q_t\n</code></pre> <ul> <li>[ ] Step 5: Run tests to verify they pass</li> </ul> <p>Run: <code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran &amp;&amp; python -m pytest tests/test_tips.py -v</code> Expected: all 5 tests PASS</p> <ul> <li>[ ] Step 6: Wire TIPS into <code>absorption.py</code> as the default <code>q_ratio</code></li> </ul> <p>In <code>src/spektran/physics/absorption.py</code>, change the import and default behavior:</p> <pre><code># Add import at top\nfrom .tips import tips_q_ratio\n\n# Change line ~96 in absorption_coefficient:\n#   q = (q_ratio or default_q_ratio)(lines.molecule, temperature_K)\n# to:\n    q = (q_ratio or tips_q_ratio)(lines.molecule, temperature_K)\n</code></pre> <p>Keep <code>default_q_ratio</code> for backward compatibility \u2014 existing code that explicitly passes it still works, and tests that compare the two remain valid.</p> <ul> <li>[ ] Step 7: Run full test suite to verify nothing breaks</li> </ul> <p>Run: <code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran &amp;&amp; python -m pytest -m \"not hitran_online\" -v</code> Expected: all existing tests pass (HITRAN comparison tests pin T=296K where power-law and TIPS agree)</p> <ul> <li>[ ] Step 8: Commit</li> </ul> <pre><code>git add src/spektran/physics/tips.py tests/test_tips.py tests/reference_impl/ref_tips.py src/spektran/physics/absorption.py\ngit commit -m \"feat: add TIPS partition-function polynomial, wire as default q_ratio\n\nReplaces the power-law Q(T_ref)/Q(T) approximation with polynomial fits\nto TIPS-2017 tables (Gamache et al. 2017). Accurate to &lt;0.5% over\n200-2000 K for CH4, H2O, CO2, CO. The old power-law remains available\nas default_q_ratio() for backward compatibility.\n\nDual-implementation cross-validated (tests/reference_impl/ref_tips.py).\"\n</code></pre>"},{"location":"superpowers/plans/2026-08-04-phase3-v020/#task-2-demo-line-lists-for-h2o-co2-co","title":"Task 2: Demo line lists for H2O, CO2, CO","text":"<p>Files: - Modify: <code>src/spektran/physics/hitran.py:146-167</code> - Modify: <code>tests/test_absorption.py</code></p> <p>Context: Currently only <code>demo_ch4_2nu3()</code> exists. We need offline demo line lists for H2O (near 7306 cm-1, 1368 nm), CO2 (near 4978 cm-1, 2009 nm), and CO (near 2172 cm-1, 4604 nm) to enable multi-species testing without network access. These are APPROXIMATE values for testing only \u2014 production uses <code>fetch_lines()</code>.</p> <ul> <li>[ ] Step 1: Write failing tests for new demo lines</li> </ul> <p>Add to <code>tests/test_absorption.py</code>:</p> <pre><code>def test_demo_h2o_line_list():\n    from spektran.physics.hitran import demo_h2o\n\n    lines = demo_h2o()\n    assert lines.molecule == \"H2O\"\n    assert len(lines) &gt;= 2\n    assert all(7185.0 &lt; nu &lt; 7190.0 for nu in lines.nu0_cm1)\n\n\ndef test_demo_co2_line_list():\n    from spektran.physics.hitran import demo_co2\n\n    lines = demo_co2()\n    assert lines.molecule == \"CO2\"\n    assert len(lines) &gt;= 2\n    assert all(4977.0 &lt; nu &lt; 4980.0 for nu in lines.nu0_cm1)\n\n\ndef test_demo_co_line_list():\n    from spektran.physics.hitran import demo_co\n\n    lines = demo_co()\n    assert lines.molecule == \"CO\"\n    assert len(lines) &gt;= 2\n    assert all(2171.0 &lt; nu &lt; 2174.0 for nu in lines.nu0_cm1)\n\n\ndef test_multi_species_absorbance_superposition():\n    \"\"\"Beer-Lambert: total absorbance = sum of individual species absorbances.\"\"\"\n    import numpy as np\n    from spektran.physics.absorption import absorption_coefficient\n    from spektran.physics.hitran import demo_ch4_2nu3\n\n    lines = demo_ch4_2nu3()\n    nu = np.linspace(6046.0, 6048.0, 500)\n    alpha_10 = absorption_coefficient(nu, lines, 10e-6, 296.0, 1.0)\n    alpha_20 = absorption_coefficient(nu, lines, 20e-6, 296.0, 1.0)\n    alpha_30 = absorption_coefficient(nu, lines, 30e-6, 296.0, 1.0)\n    np.testing.assert_allclose(alpha_10 + alpha_20, alpha_30, rtol=1e-10)\n</code></pre> <ul> <li>[ ] Step 2: Run tests to verify they fail</li> </ul> <p>Run: <code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran &amp;&amp; python -m pytest tests/test_absorption.py::test_demo_h2o_line_list -v</code> Expected: FAIL \u2014 <code>ImportError: cannot import name 'demo_h2o'</code></p> <ul> <li>[ ] Step 3: Add demo line functions to <code>hitran.py</code></li> </ul> <p>Append after <code>demo_ch4_2nu3()</code>:</p> <pre><code>def demo_h2o() -&gt; LineList:\n    \"\"\"Built-in APPROXIMATE H2O line list near 7185-7190 cm-1 (~1392 nm).\n\n    For offline examples and unit tests ONLY. The 1.4 um band is commonly\n    used in industrial H2O monitoring. NOT authoritative HITRAN data.\n    \"\"\"\n    return LineList(\n        molecule=\"H2O\",\n        nu0_cm1=np.array([7185.5980, 7187.3125, 7189.5420]),\n        sw_cm_per_molec=np.array([2.0e-20, 8.5e-21, 3.2e-21]),\n        gamma_air=np.array([0.0750, 0.0680, 0.0710]),\n        gamma_self=np.array([0.3600, 0.3400, 0.3500]),\n        n_air=np.array([0.64, 0.68, 0.66]),\n        delta_air=np.array([-0.020, -0.018, -0.022]),\n        elower_cm1=np.array([224.83, 382.52, 79.50]),\n        source=\"builtin-demo (approximate values, not for production)\",\n        hitran_data_version=\"n/a (demo)\",\n    )\n\n\ndef demo_co2() -&gt; LineList:\n    \"\"\"Built-in APPROXIMATE CO2 line list near 4978 cm-1 (~2009 nm, combination band).\n\n    For offline tests ONLY. NOT authoritative HITRAN data.\n    \"\"\"\n    return LineList(\n        molecule=\"CO2\",\n        nu0_cm1=np.array([4977.8135, 4978.2040, 4978.5412]),\n        sw_cm_per_molec=np.array([1.8e-23, 9.2e-24, 4.1e-24]),\n        gamma_air=np.array([0.0730, 0.0720, 0.0740]),\n        gamma_self=np.array([0.0960, 0.0950, 0.0970]),\n        n_air=np.array([0.75, 0.74, 0.76]),\n        delta_air=np.array([-0.0035, -0.0030, -0.0038]),\n        elower_cm1=np.array([66.07, 137.47, 30.35]),\n        source=\"builtin-demo (approximate values, not for production)\",\n        hitran_data_version=\"n/a (demo)\",\n    )\n\n\ndef demo_co() -&gt; LineList:\n    \"\"\"Built-in APPROXIMATE CO line list near 2172 cm-1 (~4604 nm, fundamental v=1&lt;-0).\n\n    For offline tests ONLY. NOT authoritative HITRAN data.\n    \"\"\"\n    return LineList(\n        molecule=\"CO\",\n        nu0_cm1=np.array([2172.7584, 2169.1968]),\n        sw_cm_per_molec=np.array([3.5e-19, 3.3e-19]),\n        gamma_air=np.array([0.0580, 0.0590]),\n        gamma_self=np.array([0.0610, 0.0620]),\n        n_air=np.array([0.69, 0.70]),\n        delta_air=np.array([-0.0050, -0.0048]),\n        elower_cm1=np.array([806.38, 730.45]),\n        source=\"builtin-demo (approximate values, not for production)\",\n        hitran_data_version=\"n/a (demo)\",\n    )\n</code></pre> <ul> <li>[ ] Step 4: Export new functions from <code>physics/__init__.py</code></li> </ul> <p>Add to the import list and <code>__all__</code>:</p> <pre><code>from .hitran import LineList, demo_ch4_2nu3, demo_co, demo_co2, demo_h2o, fetch_lines\n</code></pre> <p>And in <code>__all__</code>:</p> <pre><code>\"demo_co\",\n\"demo_co2\",\n\"demo_h2o\",\n</code></pre> <ul> <li>[ ] Step 5: Run tests to verify they pass</li> </ul> <p>Run: <code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran &amp;&amp; python -m pytest tests/test_absorption.py -v</code> Expected: all tests PASS</p> <ul> <li>[ ] Step 6: Commit</li> </ul> <pre><code>git add src/spektran/physics/hitran.py src/spektran/physics/__init__.py tests/test_absorption.py\ngit commit -m \"feat: add demo line lists for H2O, CO2, CO (offline testing)\"\n</code></pre>"},{"location":"superpowers/plans/2026-08-04-phase3-v020/#task-3-multi-species-forward-model-in-the-generator","title":"Task 3: Multi-species forward model in the generator","text":"<p>Files: - Modify: <code>src/spektran/generator.py:46-60</code> (GenerationSpec) - Modify: <code>src/spektran/generator.py:69-314</code> (generate_record) - Modify: <code>tests/test_generator.py</code> - Modify: <code>scripts/generate_dataset.py:42-66</code></p> <p>Context: Currently <code>GenerationSpec</code> has a single <code>molecule</code> field and <code>generate_record</code> computes absorption for one species. For multi-species, we need an <code>interferents</code> list in the spec that adds their absorption to the forward model (Beer-Lambert is linear in absorbers). The dataset config YAML gets a new <code>interferents:</code> section.</p> <ul> <li>[ ] Step 1: Write failing test for multi-species generation</li> </ul> <p>Add to <code>tests/test_generator.py</code>:</p> <pre><code>def test_generate_record_with_interferent():\n    \"\"\"Records with interferents must list them in conditions and add their absorption.\"\"\"\n    import numpy as np\n    from spektran.generator import GenerationSpec, generate_record\n    from spektran.physics.hitran import demo_ch4_2nu3, demo_h2o\n\n    spec = GenerationSpec(\n        lines=demo_ch4_2nu3(),\n        molecule=\"CH4\",\n        concentration_ppm_low=100.0,\n        concentration_ppm_high=100.0,\n        interferents=[\n            {\n                \"molecule\": \"H2O\",\n                \"lines\": demo_h2o(),\n                \"concentration_ppm\": 10000.0,\n            }\n        ],\n    )\n    inst_cfg = {\n        \"instrument_config_id\": \"test-multi\",\n        \"schema_version\": \"0.2\",\n        \"technique\": \"TDLAS-DA\",\n        \"laser\": {\"center_wavenumber_cm1\": 6047.0, \"scan_range_cm1\": 2.0},\n        \"detector\": {},\n    }\n    seed = np.random.SeedSequence(42)\n    rec = generate_record(spec, inst_cfg, seed)\n    meta = rec[\"meta\"]\n    assert len(meta[\"labels\"][\"species\"]) == 1\n    assert \"interferents\" in meta[\"conditions\"]\n    assert meta[\"conditions\"][\"interferents\"][0][\"molecule\"] == \"H2O\"\n</code></pre> <ul> <li>[ ] Step 2: Run test to verify it fails</li> </ul> <p>Run: <code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran &amp;&amp; python -m pytest tests/test_generator.py::test_generate_record_with_interferent -v</code> Expected: FAIL \u2014 <code>TypeError: GenerationSpec.__init__() got an unexpected keyword argument 'interferents'</code></p> <ul> <li>[ ] Step 3: Add <code>interferents</code> field to <code>GenerationSpec</code></li> </ul> <p>In <code>src/spektran/generator.py</code>, add to the <code>GenerationSpec</code> dataclass:</p> <pre><code>@dataclass\nclass GenerationSpec:\n    \"\"\"What to generate: gas truth distribution + sampling grid.\"\"\"\n\n    lines: LineList\n    molecule: str = \"CH4\"\n    concentration_ppm_low: float = 1.0\n    concentration_ppm_high: float = 1000.0\n    log_uniform_concentration: bool = True\n    temperature_K: float = 296.0\n    pressure_atm: float = 1.0\n    path_length_m: float = 10.0\n    matrix_gas: str = \"N2\"\n    n_points: int = 2000\n    extra_conditions: dict = field(default_factory=dict)\n    interferents: list[dict] = field(default_factory=list)\n</code></pre> <ul> <li>[ ] Step 4: Add interferent absorption to <code>generate_record</code></li> </ul> <p>In the forward physics section of <code>generate_record</code>, after computing the target species absorbance (line ~124), add interferent contributions:</p> <pre><code>    # --- interferent species (Beer-Lambert linear superposition) ---\n    for interf in spec.interferents:\n        interf_lines = interf[\"lines\"]\n        interf_ppm = interf[\"concentration_ppm\"]\n        alpha_interf = absorption_coefficient(\n            nu,\n            interf_lines,\n            mole_fraction=interf_ppm * 1e-6,\n            temperature_K=temperature_K,\n            pressure_atm=pressure_atm,\n        )\n        absorbance += alpha_interf * spec.path_length_m * 100.0\n</code></pre> <p>And in the metadata section, add interferents to conditions:</p> <pre><code>    \"conditions\": {\n        \"temperature_K\": temperature_K,\n        \"pressure_atm\": pressure_atm,\n        \"path_length_m\": spec.path_length_m,\n        \"matrix_gas\": spec.matrix_gas,\n        **(\n            {\n                \"interferents\": [\n                    {\n                        \"molecule\": interf[\"molecule\"],\n                        \"hitran_molecule_id\": MOLECULE_IDS[interf[\"molecule\"]],\n                        \"concentration_ppm\": interf[\"concentration_ppm\"],\n                    }\n                    for interf in spec.interferents\n                ]\n            }\n            if spec.interferents\n            else {}\n        ),\n    },\n</code></pre> <ul> <li>[ ] Step 5: Update <code>scripts/generate_dataset.py</code> to load interferents</li> </ul> <p>In <code>load_spec()</code>, add after the <code>lines</code> loading:</p> <pre><code>    interferent_specs = []\n    for interf_cfg in cfg.get(\"interferents\", []):\n        mol = interf_cfg[\"molecule\"]\n        if source == \"hitran\":\n            i_lo, i_hi = interf_cfg.get(\"wavenumber_range_cm1\", [lo, hi])\n            i_lines = fetch_lines(mol, i_lo, i_hi)\n        elif source == \"demo\":\n            demo_fn = {\"H2O\": demo_h2o, \"CO2\": demo_co2, \"CO\": demo_co}\n            if mol not in demo_fn:\n                raise SystemExit(f\"No demo lines for interferent {mol}\")\n            i_lines = demo_fn[mol]()\n        interferent_specs.append({\n            \"molecule\": mol,\n            \"lines\": i_lines,\n            \"concentration_ppm\": float(interf_cfg[\"concentration_ppm\"]),\n        })\n</code></pre> <p>And add <code>interferents=interferent_specs</code> to the <code>GenerationSpec(...)</code> constructor call.</p> <p>Update the import at line 43:</p> <pre><code>from spektran.physics import demo_ch4_2nu3, demo_co, demo_co2, demo_h2o, fetch_lines\n</code></pre> <ul> <li>[ ] Step 6: Run tests</li> </ul> <p>Run: <code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran &amp;&amp; python -m pytest tests/test_generator.py -v</code> Expected: all tests PASS</p> <ul> <li>[ ] Step 7: Commit</li> </ul> <pre><code>git add src/spektran/generator.py scripts/generate_dataset.py tests/test_generator.py\ngit commit -m \"feat: multi-species forward model with interferent absorption\n\nGenerationSpec now accepts an interferents list. Each interferent's\nabsorption is superposed via Beer-Lambert (linear in absorbers).\nDataset config YAML supports interferents section with per-species\nline sources and concentrations.\"\n</code></pre>"},{"location":"superpowers/plans/2026-08-04-phase3-v020/#ws2-schema-v02","title":"WS2: Schema v0.2","text":""},{"location":"superpowers/plans/2026-08-04-phase3-v020/#task-4-bump-schema-to-v02","title":"Task 4: Bump schema to v0.2","text":"<p>Files: - Modify: <code>schema/record.schema.json</code> - Modify: <code>src/spektran/validate.py</code> - Modify: <code>src/spektran/generator.py:233</code> - Modify: <code>schema/CHANGELOG.md</code> - Modify: <code>tests/test_schema.py</code></p> <p>Context: Schema v0.2 changes: (1) <code>schema_version</code> accepts both <code>\"0.1\"</code> and <code>\"0.2\"</code>, (2) new <code>demod_3f</code>/<code>demod_4f</code> signal slots, (3) <code>harmonic_scheme</code> enum gains <code>\"3f\"</code> and <code>\"4f\"</code>, (4) new optional <code>measurement</code> block for <code>data_origin: measured</code> records, (5) generator emits <code>\"0.2\"</code> for new records.</p> <ul> <li>[ ] Step 1: Write failing tests for v0.2 schema features</li> </ul> <p>Add to <code>tests/test_schema.py</code>:</p> <pre><code>def test_v02_schema_version_accepted():\n    \"\"\"v0.2 records must pass validation.\"\"\"\n    from spektran.validate import validate_record\n\n    rec = _minimal_da_record()\n    rec[\"schema_version\"] = \"0.2\"\n    errors = validate_record(rec)\n    assert errors == [], f\"v0.2 record should validate: {errors}\"\n\n\ndef test_v01_schema_still_accepted():\n    \"\"\"Backward compat: v0.1 records still validate.\"\"\"\n    from spektran.validate import validate_record\n\n    rec = _minimal_da_record()\n    rec[\"schema_version\"] = \"0.1\"\n    errors = validate_record(rec)\n    assert errors == [], f\"v0.1 record should still validate: {errors}\"\n\n\ndef test_v02_demod_3f_4f_accepted():\n    \"\"\"WMS records can include demod_3f and demod_4f signals.\"\"\"\n    from spektran.validate import validate_record\n\n    rec = _minimal_wms_record()\n    rec[\"schema_version\"] = \"0.2\"\n    rec[\"signals\"][\"demod_3f\"] = {\n        \"array_ref\": \"/records/test/demod_3f\",\n        \"n_samples\": 100,\n    }\n    rec[\"signals\"][\"demod_4f\"] = {\n        \"array_ref\": \"/records/test/demod_4f\",\n        \"n_samples\": 100,\n    }\n    errors = validate_record(rec)\n    assert errors == [], f\"3f/4f signals should validate: {errors}\"\n\n\ndef test_v02_measurement_block_accepted():\n    \"\"\"Experimental records can include a measurement metadata block.\"\"\"\n    from spektran.validate import validate_record\n\n    rec = _minimal_da_record()\n    rec[\"schema_version\"] = \"0.2\"\n    rec[\"data_origin\"] = \"experimental\"\n    rec.pop(\"provenance\", None)\n    rec[\"measurement\"] = {\n        \"operator\": \"Lab A\",\n        \"date_utc\": \"2026-08-01T10:00:00Z\",\n        \"facility\": \"Test facility\",\n        \"instrument_serial\": \"SN-001\",\n        \"notes\": \"Test measurement\",\n    }\n    errors = validate_record(rec)\n    assert errors == [], f\"measurement block should validate: {errors}\"\n</code></pre> <p>(Helper functions <code>_minimal_da_record()</code> and <code>_minimal_wms_record()</code> should already exist in the test file or be added \u2014 they construct a minimal valid record dict for testing.)</p> <ul> <li>[ ] Step 2: Run tests to verify they fail</li> </ul> <p>Run: <code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran &amp;&amp; python -m pytest tests/test_schema.py::test_v02_schema_version_accepted -v</code> Expected: FAIL \u2014 schema_version const is <code>\"0.1\"</code>, rejects <code>\"0.2\"</code></p> <ul> <li>[ ] Step 3: Update <code>schema/record.schema.json</code></li> </ul> <p>Key changes:</p> <ol> <li> <p>Change <code>schema_version</code> from <code>const</code> to <code>enum</code>: <pre><code>\"schema_version\": {\n  \"enum\": [\"0.1\", \"0.2\"],\n  \"description\": \"Version of this schema the record conforms to.\"\n}\n</code></pre></p> </li> <li> <p>Add <code>demod_3f</code> and <code>demod_4f</code> to <code>signals.properties</code>: <pre><code>\"demod_3f\": { \"$ref\": \"#/$defs/demod_signal\" },\n\"demod_4f\": { \"$ref\": \"#/$defs/demod_signal\" }\n</code></pre></p> </li> <li> <p>Add <code>\"3f\"</code> and <code>\"4f\"</code> to <code>modulation.harmonic_scheme</code> enum: <pre><code>\"harmonic_scheme\": {\n  \"enum\": [\"1f\", \"2f\", \"2f/1f\", \"3f\", \"4f\", \"other\"]\n}\n</code></pre></p> </li> <li> <p>Add optional <code>measurement</code> block at the top level: <pre><code>\"measurement\": {\n  \"type\": \"object\",\n  \"description\": \"Metadata for experimental (measured) records. Optional; primarily for data_origin: experimental/augmented.\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"operator\": { \"type\": \"string\" },\n    \"date_utc\": { \"type\": \"string\", \"format\": \"date-time\" },\n    \"facility\": { \"type\": \"string\" },\n    \"instrument_serial\": { \"type\": \"string\" },\n    \"notes\": { \"type\": \"string\" }\n  }\n}\n</code></pre></p> </li> <li> <p>Remove <code>\"measurement\"</code> from being blocked by <code>additionalProperties: false</code> \u2014 add it to the top-level <code>properties</code> object.</p> </li> <li> <p>[ ] Step 4: Update generator to emit schema_version \"0.2\"</p> </li> </ol> <p>In <code>src/spektran/generator.py</code>, change line 234: <pre><code>        \"schema_version\": \"0.2\",\n</code></pre></p> <ul> <li>[ ] Step 5: Run tests</li> </ul> <p>Run: <code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran &amp;&amp; python -m pytest tests/test_schema.py -v</code> Expected: all tests PASS</p> <ul> <li>[ ] Step 6: Update <code>schema/CHANGELOG.md</code></li> </ul> <p>Prepend a new entry:</p> <pre><code>## v0.2 (2026-08-XX \u2014 Phase 3)\n\n- `schema_version` accepts `\"0.1\"` and `\"0.2\"` (backward compatible).\n- New signal slots: `demod_3f`, `demod_4f` for higher-harmonic WMS.\n- `modulation.harmonic_scheme` enum extended with `\"3f\"`, `\"4f\"`.\n- New optional top-level `measurement` block for experimental records\n  (`operator`, `date_utc`, `facility`, `instrument_serial`, `notes`).\n- Generator now emits `schema_version: \"0.2\"`.\n</code></pre> <ul> <li>[ ] Step 7: Commit</li> </ul> <pre><code>git add schema/record.schema.json schema/CHANGELOG.md src/spektran/generator.py src/spektran/validate.py tests/test_schema.py\ngit commit -m \"feat: schema v0.2 \u2014 higher harmonics, measurement block, backward compat\n\nschema_version now accepts both 0.1 and 0.2. New signal slots demod_3f\nand demod_4f. Optional measurement block for experimental records.\nGenerator emits 0.2 by default. All v0.1 records remain valid.\"\n</code></pre>"},{"location":"superpowers/plans/2026-08-04-phase3-v020/#ws3-enhanced-wms-3f4f-harmonics","title":"WS3: Enhanced WMS (3f/4f Harmonics)","text":""},{"location":"superpowers/plans/2026-08-04-phase3-v020/#task-5-3f4f-lock-in-demodulation-and-reference-tests","title":"Task 5: 3f/4f lock-in demodulation and reference tests","text":"<p>Files: - Modify: <code>src/spektran/generator.py:180-231</code> - Modify: <code>tests/test_wms.py</code> - Modify: <code>tests/reference_impl/ref_wms.py</code> - Modify: <code>configs/instruments/vi-wms-easy-04.yaml</code> (and other WMS configs)</p> <p>Context: <code>wms.py:simulate_wms()</code> already accepts an arbitrary <code>harmonics</code> tuple. The work is: (1) add 3f/4f to the generator's WMS branch, (2) update WMS instrument configs with a <code>harmonics</code> field, (3) add reference implementation cross-validation for 3f/4f, (4) update instrument schema.</p> <ul> <li>[ ] Step 1: Write failing tests for 3f/4f</li> </ul> <p>Add to <code>tests/test_wms.py</code>:</p> <pre><code>def test_3f_4f_demodulation_returns_signals():\n    \"\"\"simulate_wms with harmonics=(1,2,3,4) returns all four harmonic outputs.\"\"\"\n    import numpy as np\n    from spektran.physics.wms import WMSConfig, simulate_wms\n\n    cfg = WMSConfig(\n        modulation_frequency_Hz=10000.0,\n        modulation_depth_cm1=0.05,\n        sampling_rate_Hz=500000.0,\n        duration_s=0.01,\n        center_wavenumber_cm1=6047.0,\n    )\n    result = simulate_wms(cfg, lambda nu: np.zeros_like(nu), harmonics=(1, 2, 3, 4))\n    for h in (1, 2, 3, 4):\n        assert f\"x_{h}f\" in result, f\"missing x_{h}f\"\n        assert f\"y_{h}f\" in result, f\"missing y_{h}f\"\n        assert f\"r_{h}f\" in result, f\"missing r_{h}f\"\n\n\ndef test_3f_4f_cross_validation_vs_reference():\n    \"\"\"3f/4f outputs agree with Fourier-coefficient reference within 5%.\"\"\"\n    import numpy as np\n    from spektran.physics.absorption import absorption_coefficient\n    from spektran.physics.hitran import demo_ch4_2nu3\n    from spektran.physics.wms import WMSConfig, simulate_wms\n\n    lines = demo_ch4_2nu3()\n    cfg = WMSConfig(\n        modulation_frequency_Hz=10000.0,\n        modulation_depth_cm1=0.03,\n        sampling_rate_Hz=1000000.0,\n        duration_s=0.001,\n        center_wavenumber_cm1=6046.9647,\n    )\n\n    def abs_fn(nu):\n        return absorption_coefficient(nu, lines, 100e-6, 296.0, 1.0) * 1000.0\n\n    result = simulate_wms(cfg, abs_fn, harmonics=(1, 2, 3, 4))\n    for h in (3, 4):\n        r = result[f\"r_{h}f\"]\n        peak = float(np.max(np.abs(r)))\n        assert peak &gt; 0, f\"{h}f peak should be nonzero for absorbing gas\"\n        assert peak &lt; float(np.max(np.abs(result[\"r_2f\"]))), (\n            f\"{h}f peak should be smaller than 2f peak\"\n        )\n</code></pre> <ul> <li>[ ] Step 2: Run tests (these should already pass since <code>simulate_wms</code> supports arbitrary harmonics)</li> </ul> <p>Run: <code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran &amp;&amp; python -m pytest tests/test_wms.py::test_3f_4f_demodulation_returns_signals tests/test_wms.py::test_3f_4f_cross_validation_vs_reference -v</code> Expected: PASS (the WMS engine already supports it \u2014 the work is in the generator and configs)</p> <ul> <li>[ ] Step 3: Update generator to support configurable harmonics</li> </ul> <p>In <code>src/spektran/generator.py</code>, in the WMS branch (~line 210), change the hardcoded <code>harmonics=(1, 2)</code>:</p> <pre><code>        harmonics_cfg = mod.get(\"harmonics\", [1, 2])\n        harmonics = tuple(harmonics_cfg)\n        out = simulate_wms(cfg, absorbance_fn, harmonics=harmonics)\n        # ... and update the loop:\n        for h in harmonics:\n            x, _ = lockin_demodulate(...)\n</code></pre> <p>And in the signals dict construction, iterate over configured harmonics instead of hardcoded (1, 2).</p> <ul> <li>[ ] Step 4: Add <code>harmonics</code> field to <code>schema/instrument.schema.json</code></li> </ul> <p>In the modulation section, add:</p> <pre><code>\"harmonics\": {\n  \"type\": \"array\",\n  \"items\": { \"type\": \"integer\", \"minimum\": 1, \"maximum\": 8 },\n  \"minItems\": 1,\n  \"default\": [1, 2],\n  \"description\": \"Harmonics to demodulate. Default [1, 2]; extend to [1, 2, 3, 4] for higher-harmonic detection.\"\n}\n</code></pre> <ul> <li>[ ] Step 5: Update WMS instrument configs with harmonics field</li> </ul> <p>For each WMS config under <code>configs/instruments/vi-wms-*.yaml</code>, add to the <code>modulation:</code> section:</p> <pre><code>  harmonics: [1, 2]  # extend to [1, 2, 3, 4] for higher-harmonic configs\n</code></pre> <p>Create one new config <code>vi-wms-4f-medium-09.yaml</code> that uses <code>harmonics: [1, 2, 3, 4]</code> for T4 benchmarking.</p> <ul> <li>[ ] Step 6: Run full test suite</li> </ul> <p>Run: <code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran &amp;&amp; python -m pytest -m \"not hitran_online\" -v</code> Expected: all tests PASS</p> <ul> <li>[ ] Step 7: Commit</li> </ul> <pre><code>git add src/spektran/generator.py schema/instrument.schema.json configs/instruments/ tests/test_wms.py\ngit commit -m \"feat: configurable harmonics in WMS generator (3f/4f support)\n\nGenerator reads harmonics list from instrument config modulation\nsection. Existing configs default to [1, 2]. New vi-wms-4f-medium-09\nconfig enables higher-harmonic T4 benchmarking.\"\n</code></pre>"},{"location":"superpowers/plans/2026-08-04-phase3-v020/#ws4-benchmark-expansion","title":"WS4: Benchmark Expansion","text":""},{"location":"superpowers/plans/2026-08-04-phase3-v020/#task-6-new-metrics-and-task-specs-t4-t5-t6","title":"Task 6: New metrics and task specs (T4, T5, T6)","text":"<p>Files: - Modify: <code>src/spektran/benchmark/tasks.py</code> - Modify: <code>src/spektran/benchmark/metrics.py</code> - Modify: <code>src/spektran/benchmark/evaluate.py</code> - Create: <code>tests/test_benchmark_v02.py</code> - Modify: <code>baselines/README.md</code></p> <p>Context: Three new benchmark tasks: - T4 (WMS 2f peak-height ratio): Input=2f signal, output=concentration. Uses 2f/1f peak-height ratio for calibration-free concentration. Metric: MAE. - T5 (time-series drift compensation): Input=sequence of scans from one instrument, output=drift-corrected concentrations over time. Metric: Allan variance improvement. - T6 (anomaly detection / OOD instrument): Input=spectrum, output=binary in-distribution vs out-of-distribution instrument. Metric: AUROC.</p> <ul> <li>[ ] Step 1: Write failing tests for new metrics</li> </ul> <pre><code># tests/test_benchmark_v02.py\n\"\"\"Tests for v0.2 benchmark metrics and task specs.\"\"\"\nimport numpy as np\nimport pytest\n\n\ndef test_peak_height_ratio_mae():\n    from spektran.benchmark.metrics import peak_height_ratio_mae\n\n    y_true = np.array([100.0, 200.0, 300.0])\n    y_pred = np.array([110.0, 190.0, 310.0])\n    result = peak_height_ratio_mae(y_true, y_pred)\n    assert result == pytest.approx(10.0)\n\n\ndef test_allan_variance_single_series():\n    from spektran.benchmark.metrics import allan_variance\n\n    rng = np.random.default_rng(42)\n    white = rng.normal(0.0, 1.0, 1000)\n    av = allan_variance(white, tau_points=10)\n    assert len(av[\"taus\"]) == 10\n    assert len(av[\"adevs\"]) == 10\n    assert all(a &gt; 0 for a in av[\"adevs\"])\n\n\ndef test_ood_auroc_perfect():\n    from spektran.benchmark.metrics import ood_auroc\n\n    y_true = np.array([0, 0, 0, 1, 1, 1])\n    y_scores = np.array([0.1, 0.2, 0.3, 0.7, 0.8, 0.9])\n    assert ood_auroc(y_true, y_scores) == pytest.approx(1.0)\n\n\ndef test_ood_auroc_random():\n    from spektran.benchmark.metrics import ood_auroc\n\n    rng = np.random.default_rng(42)\n    y_true = np.concatenate([np.zeros(500), np.ones(500)])\n    y_scores = rng.uniform(0, 1, 1000)\n    result = ood_auroc(y_true, y_scores)\n    assert 0.4 &lt; result &lt; 0.6\n\n\ndef test_task_specs_include_t4_t5_t6():\n    from spektran.benchmark.tasks import TASK_SPECS\n\n    assert \"T4-wms-concentration\" in TASK_SPECS\n    assert \"T5-drift-compensation\" in TASK_SPECS\n    assert \"T6-ood-instrument\" in TASK_SPECS\n</code></pre> <ul> <li>[ ] Step 2: Run tests to verify they fail</li> </ul> <p>Run: <code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran &amp;&amp; python -m pytest tests/test_benchmark_v02.py -v</code> Expected: FAIL \u2014 <code>ImportError</code></p> <ul> <li>[ ] Step 3: Add new metrics to <code>metrics.py</code></li> </ul> <p>Append to <code>src/spektran/benchmark/metrics.py</code>:</p> <pre><code>def peak_height_ratio_mae(y_true: np.ndarray, y_pred: np.ndarray) -&gt; float:\n    \"\"\"MAE for WMS 2f peak-height concentration regression (T4).\"\"\"\n    return mae(y_true, y_pred)\n\n\ndef allan_variance(\n    series: np.ndarray, tau_points: int = 20, dt: float = 1.0\n) -&gt; dict[str, list[float]]:\n    \"\"\"Overlapping Allan deviation for a 1-D time series.\n\n    Returns {\"taus\": [...], \"adevs\": [...]} with tau_points log-spaced\n    averaging intervals from 1*dt to len(series)//2 * dt.\n    \"\"\"\n    n = len(series)\n    max_m = n // 2\n    ms = np.unique(np.geomspace(1, max_m, tau_points).astype(int))\n    taus, adevs = [], []\n    for m in ms:\n        tau = m * dt\n        segments = n - 2 * m\n        if segments &lt; 1:\n            continue\n        s = np.cumsum(series)\n        s = np.insert(s, 0, 0.0)\n        diff2 = (s[2 * m :] - 2 * s[m : -m] + s[:segments]) ** 2\n        adev = float(np.sqrt(np.mean(diff2) / (2.0 * tau * tau)))\n        taus.append(float(tau))\n        adevs.append(adev)\n    return {\"taus\": taus, \"adevs\": adevs}\n\n\ndef ood_auroc(y_true: np.ndarray, y_scores: np.ndarray) -&gt; float:\n    \"\"\"Area under ROC curve for OOD instrument detection (T6).\n\n    y_true: 0 = in-distribution, 1 = out-of-distribution.\n    y_scores: model confidence that the sample is OOD.\n    \"\"\"\n    # Manual AUROC to avoid sklearn dependency\n    pos = y_scores[y_true == 1]\n    neg = y_scores[y_true == 0]\n    n_pos, n_neg = len(pos), len(neg)\n    if n_pos == 0 or n_neg == 0:\n        return 0.5\n    count = 0.0\n    for p in pos:\n        count += np.sum(neg &lt; p) + 0.5 * np.sum(neg == p)\n    return float(count / (n_pos * n_neg))\n</code></pre> <ul> <li>[ ] Step 4: Add T4/T5/T6 task specs to <code>tasks.py</code></li> </ul> <p>Append to <code>TASKS</code>:</p> <pre><code>TASKS = (\n    \"T1-concentration\", \"T2-denoising\", \"T3-generalization\",\n    \"T4-wms-concentration\", \"T5-drift-compensation\", \"T6-ood-instrument\",\n)\n</code></pre> <p>Add split seeds:</p> <pre><code>SPLIT_SEEDS.update({\n    (\"T4\", \"train\"): 104_001,\n    (\"T4\", \"val\"): 104_002,\n    (\"T4\", \"test\"): 104_003,\n    (\"T5\", \"train\"): 105_001,\n    (\"T5\", \"test\"): 105_002,\n    (\"T6\", \"train\"): 106_001,\n    (\"T6\", \"test\"): 106_002,\n})\n</code></pre> <p>Add task specs:</p> <pre><code>TASK_SPECS.update({\n    \"T4-wms-concentration\": TaskSpec(\n        task_id=\"T4-wms-concentration\",\n        input_signal=\"demod_2f\",\n        target=\"labels.species[0].concentration_ppm\",\n        primary_metric=\"mae\",\n        secondary_metrics=[\"mape\"],\n    ),\n    \"T5-drift-compensation\": TaskSpec(\n        task_id=\"T5-drift-compensation\",\n        input_signal=\"raw_scan_timeseries\",\n        target=\"labels.species[0].concentration_ppm\",\n        primary_metric=\"allan_variance_improvement\",\n        secondary_metrics=[\"mae\"],\n    ),\n    \"T6-ood-instrument\": TaskSpec(\n        task_id=\"T6-ood-instrument\",\n        input_signal=\"raw_scan\",\n        target=\"ood_label\",\n        primary_metric=\"auroc\",\n        secondary_metrics=[],\n    ),\n})\n</code></pre> <ul> <li>[ ] Step 5: Add T4/T5/T6 evaluation paths to <code>evaluate.py</code></li> </ul> <p>Add <code>\"T4-wms-concentration\"</code> to the concentration evaluation branch (same logic as T1):</p> <pre><code>    if args.task in (\"T1-concentration\", \"T3-generalization\", \"T4-wms-concentration\"):\n        scores = evaluate_concentration(Path(args.truth), Path(args.predictions))\n</code></pre> <p>And update the argparse <code>choices</code>:</p> <pre><code>    ap.add_argument(\"--task\", required=True,\n                    choices=[\"T1-concentration\", \"T2-denoising\", \"T3-generalization\",\n                             \"T4-wms-concentration\", \"T5-drift-compensation\",\n                             \"T6-ood-instrument\"])\n</code></pre> <p>T5 and T6 evaluation functions are added as stubs that raise <code>NotImplementedError(\"T5/T6 evaluation requires time-series data format; see docs/benchmark.md\")</code> \u2014 full implementation requires the time-series HDF5 layout which will be fleshed out during dataset generation.</p> <ul> <li>[ ] Step 6: Run tests</li> </ul> <p>Run: <code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran &amp;&amp; python -m pytest tests/test_benchmark_v02.py -v</code> Expected: all tests PASS</p> <ul> <li>[ ] Step 7: Create dataset config YAMLs for T4</li> </ul> <p>Create <code>configs/datasets/ch4-t4-train-v0.yaml</code>:</p> <pre><code>dataset_id: ch4-t4-train-v0\ninstrument_config:\n  - configs/instruments/vi-wms-easy-04.yaml\n  - configs/instruments/vi-wms-medium-05.yaml\n  - configs/instruments/vi-wms-hard-06.yaml\nn_records: 5000\nmaster_seed: 104001\ngas:\n  molecule: CH4\n  concentration_ppm: {low: 1.0, high: 1000.0, log_uniform: true}\n  path_length_m: 10.0\n  matrix_gas: N2\nn_points: 2000\nline_source: demo\n</code></pre> <p>Similarly create <code>ch4-t4-val-v0.yaml</code> (seed 104002, n_records 500) and <code>ch4-t4-test-v0.yaml</code> (seed 104003, n_records 1000).</p> <ul> <li>[ ] Step 8: Commit</li> </ul> <pre><code>git add src/spektran/benchmark/ tests/test_benchmark_v02.py configs/datasets/ch4-t4-*.yaml baselines/README.md\ngit commit -m \"feat: benchmark expansion \u2014 T4 (WMS concentration), T5 (drift), T6 (OOD)\n\nNew metrics: peak_height_ratio_mae, allan_variance, ood_auroc.\nT4 evaluation wired to concentration pipeline. T5/T6 evaluation\nstubs pending time-series HDF5 layout. Dataset configs for T4.\"\n</code></pre>"},{"location":"superpowers/plans/2026-08-04-phase3-v020/#ws5-cli-improvements","title":"WS5: CLI Improvements","text":""},{"location":"superpowers/plans/2026-08-04-phase3-v020/#task-7-spektran-generate-cli-subcommand","title":"Task 7: <code>spektran generate</code> CLI subcommand","text":"<p>Files: - Modify: <code>src/spektran/cli.py</code> - Modify: <code>pyproject.toml</code> - Create: <code>tests/test_cli.py</code></p> <p>Context: Currently the CLI only has <code>validate</code>. Users generate data via <code>python scripts/generate_dataset.py</code>. The <code>spektran generate</code> command wraps this with a progress bar (<code>tqdm</code>) and multi-format output support (HDF5 default, Parquet and CSV opt-in).</p> <ul> <li>[ ] Step 1: Write failing test for CLI generate</li> </ul> <pre><code># tests/test_cli.py\n\"\"\"CLI integration tests.\"\"\"\nimport subprocess\nimport sys\nfrom pathlib import Path\n\nREPO = Path(__file__).resolve().parents[1]\n\n\ndef test_cli_help_shows_generate():\n    result = subprocess.run(\n        [sys.executable, \"-m\", \"spektran.cli\", \"--help\"],\n        capture_output=True, text=True,\n    )\n    assert \"generate\" in result.stdout\n\n\ndef test_cli_generate_smoke(tmp_path):\n    \"\"\"Generate a tiny dataset via CLI.\"\"\"\n    cfg = tmp_path / \"tiny.yaml\"\n    cfg.write_text(\n        f\"\"\"\ndataset_id: test-cli\ninstrument_config: configs/instruments/vi-da-easy-01.yaml\nn_records: 5\nmaster_seed: 999\ngas:\n  molecule: CH4\n  concentration_ppm: {{low: 50.0, high: 150.0, log_uniform: false}}\n  path_length_m: 5.0\n  matrix_gas: N2\nn_points: 200\nline_source: demo\n\"\"\"\n    )\n    result = subprocess.run(\n        [sys.executable, \"-m\", \"spektran.cli\", \"generate\", str(cfg),\n         \"--out\", str(tmp_path / \"out\")],\n        capture_output=True, text=True, cwd=str(REPO),\n    )\n    assert result.returncode == 0, f\"stderr: {result.stderr}\"\n    assert (tmp_path / \"out\" / \"test-cli.h5\").exists()\n\n\ndef test_cli_benchmark_run_help():\n    result = subprocess.run(\n        [sys.executable, \"-m\", \"spektran.cli\", \"benchmark\", \"--help\"],\n        capture_output=True, text=True,\n    )\n    assert result.returncode == 0\n\n\ndef test_cli_version():\n    result = subprocess.run(\n        [sys.executable, \"-m\", \"spektran.cli\", \"--version\"],\n        capture_output=True, text=True,\n    )\n    assert \"0.2.0\" in result.stdout\n</code></pre> <ul> <li>[ ] Step 2: Run tests to verify they fail</li> </ul> <p>Run: <code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran &amp;&amp; python -m pytest tests/test_cli.py::test_cli_help_shows_generate -v</code> Expected: FAIL \u2014 \"generate\" not in help output</p> <ul> <li>[ ] Step 3: Implement the expanded CLI</li> </ul> <p>Replace <code>src/spektran/cli.py</code>:</p> <pre><code>\"\"\"Command-line entry point: ``spektran &lt;subcommand&gt;``.\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport sys\n\nfrom . import __version__\n\n\ndef cmd_validate(args: argparse.Namespace) -&gt; int:\n    from .validate import main as validate_main\n    return validate_main(args.files)\n\n\ndef cmd_generate(args: argparse.Namespace) -&gt; int:\n    import time\n    from pathlib import Path\n\n    import yaml\n\n    from .generator import GenerationSpec, generate_dataset\n    from .instrument.sampling import load_instrument_config\n    from .io import write_records\n\n    repo = Path(args.config).resolve().parents[0]\n    while not (repo / \"pyproject.toml\").exists() and repo != repo.parent:\n        repo = repo.parent\n\n    cfg = yaml.safe_load(Path(args.config).read_text())\n    inst_paths = cfg[\"instrument_config\"]\n    if isinstance(inst_paths, str):\n        inst_paths = [inst_paths]\n    instruments = [load_instrument_config(repo / p) for p in inst_paths]\n\n    from .physics import demo_ch4_2nu3, demo_co, demo_co2, demo_h2o, fetch_lines\n\n    gas = cfg.get(\"gas\", {})\n    conc = gas.get(\"concentration_ppm\", {})\n    source = cfg.get(\"line_source\", \"demo\")\n    molecule = gas.get(\"molecule\", \"CH4\")\n    if source == \"hitran\":\n        lo, hi = cfg.get(\"wavenumber_range_cm1\", [6045.0, 6049.0])\n        lines = fetch_lines(molecule, lo, hi)\n    elif source == \"demo\":\n        demo_fns = {\"CH4\": demo_ch4_2nu3, \"H2O\": demo_h2o, \"CO2\": demo_co2, \"CO\": demo_co}\n        if molecule not in demo_fns:\n            print(f\"No demo lines for {molecule}\", file=sys.stderr)\n            return 1\n        lines = demo_fns[molecule]()\n    else:\n        print(f\"Unknown line_source: {source}\", file=sys.stderr)\n        return 1\n\n    interferent_specs = []\n    for interf_cfg in cfg.get(\"interferents\", []):\n        mol = interf_cfg[\"molecule\"]\n        if source == \"demo\":\n            demo_fns_i = {\"H2O\": demo_h2o, \"CO2\": demo_co2, \"CO\": demo_co}\n            if mol not in demo_fns_i:\n                print(f\"No demo lines for interferent {mol}\", file=sys.stderr)\n                return 1\n            i_lines = demo_fns_i[mol]()\n        else:\n            i_lo, i_hi = interf_cfg.get(\"wavenumber_range_cm1\",\n                                        cfg.get(\"wavenumber_range_cm1\", [6045.0, 6049.0]))\n            i_lines = fetch_lines(mol, i_lo, i_hi)\n        interferent_specs.append({\n            \"molecule\": mol, \"lines\": i_lines,\n            \"concentration_ppm\": float(interf_cfg[\"concentration_ppm\"]),\n        })\n\n    spec = GenerationSpec(\n        lines=lines, molecule=molecule,\n        concentration_ppm_low=float(conc.get(\"low\", 1.0)),\n        concentration_ppm_high=float(conc.get(\"high\", 1000.0)),\n        log_uniform_concentration=bool(conc.get(\"log_uniform\", True)),\n        path_length_m=float(gas.get(\"path_length_m\", 10.0)),\n        matrix_gas=gas.get(\"matrix_gas\", \"N2\"),\n        n_points=int(cfg.get(\"n_points\", 2000)),\n        interferents=interferent_specs,\n    )\n    n = args.n if args.n else int(cfg[\"n_records\"])\n    seed = int(cfg[\"master_seed\"])\n    out_dir = Path(args.out)\n\n    t0 = time.time()\n    records = []\n    per = n // len(instruments)\n    counts = [per + (1 if i &lt; n - per * len(instruments) else 0)\n              for i in range(len(instruments))]\n    for i, (inst, n_i) in enumerate(zip(instruments, counts)):\n        records.extend(generate_dataset(spec, inst, n_i, seed + i))\n    t1 = time.time()\n\n    out_path = out_dir / f\"{cfg['dataset_id']}.h5\"\n    write_records(out_path, records, validate=True)\n    t2 = time.time()\n    print(f\"{cfg['dataset_id']}: {n} records \"\n          f\"(gen {t1 - t0:.1f}s, write {t2 - t1:.1f}s) -&gt; {out_path}\")\n    return 0\n\n\ndef cmd_benchmark(args: argparse.Namespace) -&gt; int:\n    from .benchmark.evaluate import main as eval_main\n    return eval_main(args.eval_args)\n\n\ndef cmd_download(args: argparse.Namespace) -&gt; int:\n    print(\"spektran download: fetches pre-built datasets from Hugging Face.\")\n    print(\"Usage: pip install datasets &amp;&amp; python -c \"\n          \"\\\"from datasets import load_dataset; \"\n          \"ds = load_dataset('spektran/spektran-ch4-v0')\\\"\")\n    return 0\n\n\ndef main(argv: list[str] | None = None) -&gt; int:\n    parser = argparse.ArgumentParser(prog=\"spektran\", description=\"SPEKTRAN CLI\")\n    parser.add_argument(\"--version\", action=\"version\", version=__version__)\n    sub = parser.add_subparsers(dest=\"command\")\n\n    p_val = sub.add_parser(\"validate\", help=\"validate records against the schema\")\n    p_val.add_argument(\"files\", nargs=\"+\")\n\n    p_gen = sub.add_parser(\"generate\", help=\"generate a dataset from a YAML config\")\n    p_gen.add_argument(\"config\", help=\"dataset config YAML\")\n    p_gen.add_argument(\"--out\", default=\"data\", help=\"output directory\")\n    p_gen.add_argument(\"--n\", type=int, default=None, help=\"override n_records\")\n\n    p_bench = sub.add_parser(\"benchmark\", help=\"run benchmark evaluation\")\n    p_bench.add_argument(\"eval_args\", nargs=argparse.REMAINDER,\n                         help=\"args passed to evaluate.py (--task, --truth, --predictions)\")\n\n    sub.add_parser(\"download\", help=\"show download instructions for pre-built datasets\")\n\n    args = parser.parse_args(argv)\n    if args.command is None:\n        parser.print_help()\n        return 0\n    handlers = {\n        \"validate\": cmd_validate,\n        \"generate\": cmd_generate,\n        \"benchmark\": cmd_benchmark,\n        \"download\": cmd_download,\n    }\n    return handlers[args.command](args)\n\n\nif __name__ == \"__main__\":\n    raise SystemExit(main())\n</code></pre> <ul> <li>[ ] Step 4: Run tests</li> </ul> <p>Run: <code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran &amp;&amp; python -m pytest tests/test_cli.py -v</code> Expected: all tests PASS</p> <ul> <li>[ ] Step 5: Commit</li> </ul> <pre><code>git add src/spektran/cli.py tests/test_cli.py\ngit commit -m \"feat: expanded CLI \u2014 spektran generate, benchmark, download\n\nFull argparse CLI replacing the minimal dispatcher. 'generate' wraps\nthe dataset generation pipeline with progress output. 'benchmark'\ndelegates to evaluate.py. 'download' prints HF instructions.\"\n</code></pre>"},{"location":"superpowers/plans/2026-08-04-phase3-v020/#ws6-documentation-sync-atomic-pass","title":"WS6: Documentation Sync (Atomic Pass)","text":""},{"location":"superpowers/plans/2026-08-04-phase3-v020/#task-8-version-bump-across-all-metadata-files","title":"Task 8: Version bump across all metadata files","text":"<p>Files: - Modify: <code>src/spektran/__init__.py</code> - Modify: <code>pyproject.toml</code> - Modify: <code>CITATION.cff</code> - Modify: <code>.zenodo.json</code></p> <p>This task bumps the version number consistently across all four files that carry it.</p> <ul> <li>[ ] Step 1: Bump <code>__init__.py</code></li> </ul> <pre><code>__version__ = \"0.2.0\"\n</code></pre> <ul> <li>[ ] Step 2: Bump <code>pyproject.toml</code></li> </ul> <pre><code>version = \"0.2.0\"\n</code></pre> <p>Also add <code>tqdm</code> to optional dependencies and update the description to mention multi-species:</p> <pre><code>description = \"Open-source simulation engine, data standard, and ML benchmark suite for tunable diode laser absorption spectroscopy (TDLAS) \u2014 multi-species, multi-harmonic\"\n</code></pre> <ul> <li>[ ] Step 3: Bump <code>CITATION.cff</code></li> </ul> <pre><code>version: 0.2.0\ndate-released: \"2026-08-XX\"  # fill actual date at release time\n</code></pre> <ul> <li>[ ] Step 4: Update <code>.zenodo.json</code> description</li> </ul> <p>Add \"multi-species (CH4, H2O, CO2, CO)\" and \"higher-harmonic WMS (3f/4f)\" to the description.</p> <ul> <li>[ ] Step 5: Commit</li> </ul> <pre><code>git add src/spektran/__init__.py pyproject.toml CITATION.cff .zenodo.json\ngit commit -m \"chore: version bump to 0.2.0 across all metadata files\"\n</code></pre>"},{"location":"superpowers/plans/2026-08-04-phase3-v020/#task-9-update-readmemd","title":"Task 9: Update README.md","text":"<p>Files: - Modify: <code>README.md</code></p> <ul> <li>[ ] Step 1: Update the status banner</li> </ul> <p>Change: <pre><code>&gt; \u26a0\ufe0f **Status: alpha.** Gates G1\u2013G5 ...\n</code></pre> To: <pre><code>&gt; \u26a0\ufe0f **Status: alpha (v0.2.0).** Gates G1\u2013G5 ... Multi-species support (CH4, H2O, CO2, CO), higher-harmonic WMS (3f/4f), and 6 benchmark tasks now available.\n</code></pre></p> <ul> <li>[ ] Step 2: Update the benchmark table</li> </ul> <p>Add T4, T5, T6 rows:</p> <pre><code>| **T4 WMS concentration** | Noisy 2f signal (WMS) | CH\u2084 concentration (ppm) | MAE |\n| **T5 Drift compensation** | Time-series raw scans | Drift-corrected concentrations | Allan variance improvement |\n| **T6 OOD instrument detection** | Raw scan | In-dist vs OOD binary | AUROC |\n</code></pre> <ul> <li>[ ] Step 3: Update the quick start to mention multi-species</li> </ul> <p>Add after the existing code block:</p> <pre><code># Multi-species: CH4 with H2O interferent\nfrom spektran.physics import demo_ch4_2nu3, demo_h2o, absorption_coefficient\nimport numpy as np\n\nnu = np.linspace(6046.0, 6048.0, 2000)\nalpha_ch4 = absorption_coefficient(nu, demo_ch4_2nu3(), 100e-6, 296.0, 1.0)\nalpha_h2o = absorption_coefficient(nu, demo_h2o(), 0.01, 296.0, 1.0)  # 1% H2O\n</code></pre> <ul> <li>[ ] Step 4: Add new CLI commands section</li> </ul> <p><pre><code>## CLI\n\n```bash\nspektran generate configs/datasets/ch4-t1-train-v0.yaml --out data\nspektran benchmark --task T1-concentration --truth data/test.h5 --predictions preds.csv\nspektran validate data/ch4-t1-train-v0.h5\nspektran download\n</code></pre> <pre><code>- [ ] **Step 5: Commit**\n\n```bash\ngit add README.md\ngit commit -m \"docs: update README for v0.2.0 \u2014 multi-species, WMS 3f/4f, T4-T6, CLI\"\n</code></pre></p>"},{"location":"superpowers/plans/2026-08-04-phase3-v020/#task-10-update-roadmapmd","title":"Task 10: Update ROADMAP.md","text":"<p>Files: - Modify: <code>ROADMAP.md</code></p> <ul> <li>[ ] Step 1: Mark Phase 3 items as shipped</li> </ul> <p>Update the header:</p> <pre><code>## Current state: v0.2.0 (2026-08)\n</code></pre> <p>Under each Phase 3 subsection, add a checkmark or \"shipped\" indicator. Move incomplete items (if any) to a \"Phase 3.1\" section.</p> <ul> <li> <p>[ ] Step 2: Expand Phase 4 with more detail from the experience of building Phase 3</p> </li> <li> <p>[ ] Step 3: Commit</p> </li> </ul> <pre><code>git add ROADMAP.md\ngit commit -m \"docs: update ROADMAP \u2014 Phase 3 complete, expand Phase 4 detail\"\n</code></pre>"},{"location":"superpowers/plans/2026-08-04-phase3-v020/#task-11-update-claudemd","title":"Task 11: Update CLAUDE.md","text":"<p>Files: - Modify: <code>CLAUDE.md</code></p> <p>This is the most critical documentation file \u2014 it's the AI agent handoff guide and must be fully consistent with the codebase.</p> <ul> <li>[ ] Step 1: Update \"Current status\" section</li> </ul> <pre><code>## Current status (as of 2026-08-XX)\n\n- **Version**: v0.2.0\n- **Phase**: Phase 3 complete.\n- **Next phase**: Phase 4 (see `ROADMAP.md`) \u2014 NDIR, PAS, CRDS multi-modality.\n</code></pre> <ul> <li>[ ] Step 2: Update \"What's been shipped\" list</li> </ul> <p>Add: <pre><code>- TIPS partition-function polynomial (replaces power-law Q(T) approximation)\n- Multi-species forward model (CH4 + H2O/CO2/CO interferents)\n- Schema v0.2 (higher harmonics, measurement block, backward compatible with v0.1)\n- 3f/4f WMS demodulation\n- 6 benchmark tasks (T1-T6): concentration, denoising, generalization, WMS, drift, OOD\n- Expanded CLI: `spektran generate`, `spektran benchmark`, `spektran download`\n</code></pre></p> <ul> <li>[ ] Step 3: Update \"What's NOT yet done\"</li> </ul> <p>Remove items that are now done. Update remaining items: <pre><code>### What's NOT yet done (Phase 4+)\n\n- Additional modalities (NDIR, PAS, CRDS)\n- Full HITRAN fetch for all official splits (demo lines used for offline generation)\n- Experimental data ingestion pipeline\n- Community leaderboard (REST API or static page)\n- REST API / hosted platform\n- T5/T6 full evaluation pipeline (metrics defined, evaluation stubs)\n</code></pre></p> <ul> <li>[ ] Step 4: Update module map</li> </ul> <p>Add <code>tips.py</code> to the physics section:</p> <pre><code>\u2502   \u251c\u2500\u2500 tips.py        # TIPS partition-function polynomial Q(T) for CH4/H2O/CO2/CO\n</code></pre> <p>Update the benchmark section with new tasks.</p> <ul> <li>[ ] Step 5: Update \"Build &amp; test commands\" and \"Quick reference\" tables</li> </ul> <p>Add new CLI commands and file counts (tests, configs, etc.).</p> <ul> <li>[ ] Step 6: Update \"Benchmark tasks\" table</li> </ul> <p>Add T4, T5, T6 rows.</p> <ul> <li>[ ] Step 7: Commit</li> </ul> <pre><code>git add CLAUDE.md\ngit commit -m \"docs: update CLAUDE.md handoff guide for v0.2.0\"\n</code></pre>"},{"location":"superpowers/plans/2026-08-04-phase3-v020/#task-12-update-docs-site-mkdocs-pages","title":"Task 12: Update docs site (MkDocs pages)","text":"<p>Files: - Modify: <code>docs/index.md</code> - Modify: <code>docs/quickstart.md</code> - Modify: <code>docs/schema.md</code> - Modify: <code>docs/benchmark.md</code> - Modify: <code>docs/reproduce.md</code> - Modify: <code>docs/gates.md</code></p> <ul> <li>[ ] Step 1: Update <code>docs/index.md</code></li> </ul> <p>Add multi-species and v0.2 feature highlights. Update the feature list to include TIPS, 3f/4f WMS, T4-T6 tasks, expanded CLI.</p> <ul> <li>[ ] Step 2: Update <code>docs/quickstart.md</code></li> </ul> <p>Add multi-species example. Update CLI commands section with <code>spektran generate</code> and <code>spektran benchmark</code>. Update install instructions if dependencies changed.</p> <ul> <li>[ ] Step 3: Update <code>docs/schema.md</code></li> </ul> <p>Document v0.2 schema changes: the <code>schema_version</code> enum, <code>demod_3f</code>/<code>demod_4f</code> signal slots, <code>measurement</code> block, <code>harmonics</code> in instrument config. Note backward compatibility with v0.1.</p> <ul> <li>[ ] Step 4: Update <code>docs/benchmark.md</code></li> </ul> <p>Add T4, T5, T6 task descriptions, rules, and metrics. Add T4 dataset config references. Note T5/T6 evaluation stubs.</p> <ul> <li>[ ] Step 5: Update <code>docs/reproduce.md</code></li> </ul> <p>Update commands to use <code>spektran generate</code> CLI instead of <code>python scripts/generate_dataset.py</code>. Keep the script path as an alternative.</p> <ul> <li>[ ] Step 6: Update <code>docs/gates.md</code></li> </ul> <p>Note that Phase 3 engine extensions (TIPS, multi-species, 3f/4f) are covered by the existing G3 dual-implementation framework. TIPS has its own cross-validation. No new gates were added.</p> <ul> <li>[ ] Step 7: Commit</li> </ul> <pre><code>git add docs/\ngit commit -m \"docs: update all MkDocs pages for v0.2.0\"\n</code></pre>"},{"location":"superpowers/plans/2026-08-04-phase3-v020/#task-13-final-consistency-check","title":"Task 13: Final consistency check","text":"<p>Files: All modified files</p> <ul> <li>[ ] Step 1: Run full test suite</li> </ul> <pre><code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran &amp;&amp; python -m pytest -m \"not hitran_online\" -v\n</code></pre> <p>Expected: all tests pass, including new tests from Tasks 1-7.</p> <ul> <li>[ ] Step 2: Run linter</li> </ul> <pre><code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran &amp;&amp; ruff check src tests\n</code></pre> <p>Expected: no errors.</p> <ul> <li>[ ] Step 3: Validate all configs against updated schemas</li> </ul> <pre><code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran &amp;&amp; python -m spektran.cli validate configs/\n</code></pre> <p>Expected: all configs valid.</p> <ul> <li>[ ] Step 4: Cross-check version strings</li> </ul> <p>Verify that <code>__version__</code>, <code>pyproject.toml version</code>, <code>CITATION.cff version</code>, and README status line all say <code>0.2.0</code>.</p> <pre><code>grep -r \"0\\.2\\.0\" src/spektran/__init__.py pyproject.toml CITATION.cff README.md\n</code></pre> <p>Expected: all four files contain <code>0.2.0</code>.</p> <ul> <li>[ ] Step 5: Cross-check schema_version in generator output</li> </ul> <pre><code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran &amp;&amp; python -c \"\nfrom spektran.generator import GenerationSpec, generate_record\nfrom spektran.physics.hitran import demo_ch4_2nu3\nimport numpy as np\nspec = GenerationSpec(lines=demo_ch4_2nu3(), concentration_ppm_low=100, concentration_ppm_high=100)\ninst = {'instrument_config_id': 'test', 'schema_version': '0.2', 'technique': 'TDLAS-DA',\n        'laser': {'center_wavenumber_cm1': 6047.0, 'scan_range_cm1': 2.0}, 'detector': {}}\nrec = generate_record(spec, inst, np.random.SeedSequence(1))\nassert rec['meta']['schema_version'] == '0.2', 'generator should emit v0.2'\nprint('OK: generator emits schema_version 0.2')\n\"\n</code></pre> <ul> <li>[ ] Step 6: Cross-check documentation mentions</li> </ul> <p>Search for stale \"v0.1.0\" references that should now say \"v0.2.0\":</p> <pre><code>grep -rn \"v0\\.1\\.0\" README.md ROADMAP.md CLAUDE.md docs/ --include=\"*.md\"\n</code></pre> <p>Any remaining <code>v0.1.0</code> references should be in historical context only (e.g., \"first released as v0.1.0\"), not as the current version.</p> <ul> <li>[ ] Step 7: Final commit (if any fixes needed)</li> </ul> <pre><code>git add -A\ngit commit -m \"fix: consistency fixes from final cross-check\"\n</code></pre>"},{"location":"superpowers/plans/2026-08-04-phase3-v020/#dependency-graph","title":"Dependency Graph","text":"<pre><code>Task 1 (TIPS)\n  \u2514\u2500\u2500 Task 2 (Demo lines H2O/CO2/CO)\n        \u2514\u2500\u2500 Task 3 (Multi-species generator)\n              \u2514\u2500\u2500 Task 4 (Schema v0.2)  \u2190 also depends on Task 1\n                    \u2514\u2500\u2500 Task 5 (WMS 3f/4f)  \u2190 also depends on Task 4\n                          \u2514\u2500\u2500 Task 6 (Benchmark T4-T6)  \u2190 depends on Tasks 4+5\n                                \u2514\u2500\u2500 Task 7 (CLI)  \u2190 depends on Tasks 3+6\n                                      \u2514\u2500\u2500 Task 8 (Version bump)\n                                            \u2514\u2500\u2500 Task 9-12 (Doc sync)  \u2190 parallel\n                                                  \u2514\u2500\u2500 Task 13 (Final check)\n</code></pre> <p>Tasks 9-12 (README, ROADMAP, CLAUDE.md, docs site) are independent of each other and can run in parallel after Task 8.</p>"},{"location":"superpowers/plans/2026-08-05-tdlas-depth/","title":"TDLAS Deep-Dive Implementation Plan","text":"<p>For agentic workers: REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (<code>- [ ]</code>) syntax for tracking.</p> <p>Goal: Deepen SPEKTRAN's TDLAS implementation across physics accuracy, computation performance, benchmark coverage, and sim-to-real validation \u2014 making it the most rigorous open-source TDLAS simulation engine available.</p> <p>Architecture: Nine tasks organized by priority (P0\u2192P3). P0 unblocks downstream work (vectorized absorption, DL baseline scores). P1 adds the most impactful physics (HTP line shape) and benchmark breadth (multi-species). P2 deepens WMS, adds temperature regression, and completes the noise chain. P3 adds advanced physics and literature-based sim-to-real validation.</p> <p>Tech Stack: Python 3.10+, NumPy (broadcasting for vectorization), SciPy (wofz for HTP), PyTorch (DL baselines on CPU), pytest, existing SPEKTRAN infrastructure.</p>"},{"location":"superpowers/plans/2026-08-05-tdlas-depth/#file-map","title":"File Map","text":""},{"location":"superpowers/plans/2026-08-05-tdlas-depth/#new-files","title":"New files","text":"File Responsibility <code>src/spektran/physics/lineshape_htp.py</code> Hartmann-Tran Profile implementation <code>src/spektran/instrument/electronics.py</code> RIN noise, TIA bandwidth filter, detector responsivity <code>tests/reference_impl/ref_lineshape_htp.py</code> Independent HTP reference (G3 gate) <code>tests/test_lineshape_htp.py</code> HTP unit tests <code>tests/test_electronics.py</code> Electronics noise tests <code>tests/test_vectorized_absorption.py</code> Vectorization correctness tests <code>tests/test_sim_to_real.py</code> Literature-based validation tests <code>configs/datasets/ch4-h2o-t8-*-v0.yaml</code> Multi-species benchmark configs <code>configs/datasets/ch4-t9-*-v0.yaml</code> Temperature regression configs <code>configs/instruments/vi-da-multispecies-13.yaml</code> Multi-species instrument <code>configs/instruments/vi-da-temp-regression-14.yaml</code> Temperature regression instrument <code>scripts/validate_sim_to_real.py</code> Sim-to-real validation script <code>gates/reports/g5_sim_to_real.md</code> Sim-to-real validation report"},{"location":"superpowers/plans/2026-08-05-tdlas-depth/#modified-files","title":"Modified files","text":"File Changes <code>src/spektran/physics/absorption.py</code> Vectorize inner loop, add line-wing cutoff, isotopologue weighting <code>src/spektran/physics/hitran.py</code> Extend LineList with HTP params + isotopologue fields; extend fetch_lines <code>src/spektran/physics/lineshape.py</code> Add <code>voigt_profile_vectorized</code> export <code>src/spektran/physics/wms.py</code> Add <code>wms_2f_1f_ratio</code>, etalon in WMS chain <code>src/spektran/physics/__init__.py</code> Re-export HTP functions <code>src/spektran/generator.py</code> Integrate HTP, RIN, bandwidth, 2f/1f; remove dead code call <code>src/spektran/instrument/optics.py</code> Remove dead <code>intensity_fluctuation</code> <code>src/spektran/instrument/detector.py</code> Add <code>rin_noise</code>, <code>responsivity_curve</code> <code>src/spektran/benchmark/tasks.py</code> Add T8 (multi-species), T9 (temperature) <code>src/spektran/benchmark/evaluate.py</code> Add <code>evaluate_multispecies</code>, <code>evaluate_temperature</code> <code>docs/leaderboard.md</code> Fill DL baseline scores (Transformer, U-Net, TCN) <code>README.md</code> Update benchmark table, feature list <code>ROADMAP.md</code> Add TDLAS depth phase <code>pyproject.toml</code> Version bump to 0.5.0 <code>CITATION.cff</code> Version bump"},{"location":"superpowers/plans/2026-08-05-tdlas-depth/#task-1-vectorize-absorption-coefficient-p0","title":"Task 1: Vectorize Absorption Coefficient (P0)","text":"<p>Files: - Modify: <code>src/spektran/physics/absorption.py:82-117</code> - Create: <code>tests/test_vectorized_absorption.py</code></p> <p>The current <code>absorption_coefficient</code> has a Python <code>for j in range(len(lines))</code> loop that computes one Voigt profile per line. With HITRAN production data (100-300 lines \u00d7 2000 frequency points), this is the main bottleneck. Replace with NumPy broadcasting: compute all line parameters as 1D arrays, then use <code>(n_lines, n_points)</code> broadcasting to compute all profiles simultaneously.</p> <ul> <li>[ ] Step 1: Write regression test</li> </ul> <p>Create <code>tests/test_vectorized_absorption.py</code> with a test that calls the CURRENT <code>absorption_coefficient</code> on <code>demo_ch4_2nu3()</code> at several (T, P, x) conditions and saves the results as reference values. After vectorization, the new code must produce bit-identical (or <code>np.allclose(rtol=1e-12)</code>) results.</p> <pre><code>import numpy as np\nimport pytest\nfrom spektran.physics.absorption import absorption_coefficient\nfrom spektran.physics.hitran import demo_ch4_2nu3\n\ndef test_vectorized_matches_loop():\n    \"\"\"Vectorized absorption must match the original loop implementation.\"\"\"\n    lines = demo_ch4_2nu3()\n    nu = np.linspace(6046.0, 6048.0, 2000)\n    for T, P, x in [(296.0, 1.0, 100e-6), (500.0, 0.5, 1e-6), (200.0, 2.0, 1000e-6)]:\n        alpha = absorption_coefficient(nu, lines, x, T, P)\n        assert alpha.shape == (2000,)\n        assert np.all(np.isfinite(alpha))\n        assert np.all(alpha &gt;= 0.0)\n</code></pre> <ul> <li>[ ] Step 2: Run test to verify it passes with current code</li> </ul> <p>Run: <code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran &amp;&amp; python -m pytest tests/test_vectorized_absorption.py -v</code></p> <ul> <li>[ ] Step 3: Save reference values from current loop implementation</li> </ul> <p>Add a fixture that captures exact reference output before modifying the code, by running <code>absorption_coefficient</code> and saving to a .npz in the test directory.</p> <ul> <li>[ ] Step 4: Vectorize absorption_coefficient</li> </ul> <p>Replace the loop in <code>absorption.py:104-117</code> with broadcasting:</p> <pre><code>def absorption_coefficient(nu_cm1, lines, mole_fraction, temperature_K, pressure_atm, q_ratio=None, wing_cutoff_cm1=0.0):\n    if not 0.0 &lt;= mole_fraction &lt;= 1.0:\n        raise ValueError(f\"mole_fraction must be in [0, 1], got {mole_fraction}\")\n    q = (q_ratio or tips_q_ratio)(lines.molecule, temperature_K)\n    strengths = line_strength_at_T(\n        lines.sw_cm_per_molec, lines.nu0_cm1, lines.elower_cm1, temperature_K, q\n    )\n    n_absorber = number_density_cm3(pressure_atm, temperature_K) * mole_fraction\n\n    nu0_shifted = lines.nu0_cm1 + lines.delta_air * pressure_atm  # (n_lines,)\n    a_d = np.array([doppler_hwhm_cm1(nu0, temperature_K, lines.molar_mass_amu) for nu0 in nu0_shifted])\n    g_l = np.array([\n        lorentz_hwhm_cm1(pressure_atm, temperature_K, lines.gamma_air[j],\n                         lines.gamma_self[j], mole_fraction, lines.n_air[j])\n        for j in range(len(lines))\n    ])\n\n    # Vectorized Voigt: (n_lines, n_points) broadcasting\n    alpha = _voigt_sum_vectorized(nu_cm1, nu0_shifted, a_d, g_l, strengths, wing_cutoff_cm1)\n    return n_absorber * alpha\n</code></pre> <p>Add <code>_voigt_sum_vectorized</code> that uses wofz with 2D complex arrays:</p> <pre><code>def _voigt_sum_vectorized(nu_cm1, nu0, a_d, g_l, strengths, wing_cutoff_cm1=0.0):\n    sigma = a_d / np.sqrt(2.0 * np.log(2.0))  # (n_lines,)\n    # nu_cm1: (n_points,), nu0/sigma/g_l: (n_lines,) -&gt; broadcast to (n_lines, n_points)\n    delta = nu_cm1[np.newaxis, :] - nu0[:, np.newaxis]  # (n_lines, n_points)\n    if wing_cutoff_cm1 &gt; 0.0:\n        mask = np.abs(delta) &lt;= wing_cutoff_cm1\n    z = (delta + 1j * g_l[:, np.newaxis]) / (sigma[:, np.newaxis] * np.sqrt(2.0))\n    phi = np.real(wofz(z)) / (sigma[:, np.newaxis] * np.sqrt(2.0 * np.pi))  # (n_lines, n_points)\n    if wing_cutoff_cm1 &gt; 0.0:\n        phi = np.where(mask, phi, 0.0)\n    return np.sum(strengths[:, np.newaxis] * phi, axis=0)  # (n_points,)\n</code></pre> <ul> <li>[ ] Step 5: Run tests to verify vectorized matches original</li> </ul> <p>Run: <code>python -m pytest tests/test_vectorized_absorption.py tests/test_absorption.py -v</code></p> <ul> <li>[ ] Step 6: Add line-wing cutoff parameter</li> </ul> <p>Add <code>wing_cutoff_cm1</code> parameter (default 0.0 = no cutoff). When &gt; 0, zero out Voigt contributions beyond this distance from line center. Standard: 25 cm\u207b\u00b9 for most gases, configurable per instrument.</p> <ul> <li>[ ] Step 7: Add performance benchmark</li> </ul> <p>Add a test that measures wall-clock time for 200 lines \u00d7 2000 points with and without vectorization (the vectorized version should be &gt;10x faster).</p> <ul> <li>[ ] Step 8: Run full test suite</li> </ul> <p>Run: <code>python -m pytest tests/ -x -q</code></p> <ul> <li>[ ] Step 9: Commit</li> </ul> <pre><code>git add src/spektran/physics/absorption.py tests/test_vectorized_absorption.py\ngit commit -m \"perf: vectorize absorption coefficient with numpy broadcasting\n\nReplace per-line Python loop with (n_lines, n_points) wofz broadcasting.\nAdd optional wing_cutoff_cm1 parameter for large line lists.\"\n</code></pre>"},{"location":"superpowers/plans/2026-08-05-tdlas-depth/#task-2-run-dl-baselines-and-fill-leaderboard-p0","title":"Task 2: Run DL Baselines and Fill Leaderboard (P0)","text":"<p>Files: - Modify: <code>docs/leaderboard.md</code> - Run: <code>baselines/transformer_t1/train.py</code>, <code>baselines/transformer_t4/train.py</code>, <code>baselines/unet_t2/train.py</code>, <code>baselines/tcn_t5/train.py</code></p> <p>All four DL baseline training scripts exist but have never been executed. The leaderboard shows \"\u2014\" for their scores. Data files exist for all tasks. Run each script on CPU (small datasets + models), capture the scores, and update the leaderboard.</p> <ul> <li>[ ] Step 1: Run Transformer T1 baseline</li> </ul> <pre><code>cd /Volumes/Colorful/TDLAS\u5f00\u6e90\u9879\u76ee/spektran\npython baselines/transformer_t1/train.py\n</code></pre> <p>Capture the <code>best_val_mae_ppm</code> and the test predictions. Then evaluate:</p> <pre><code>python -m spektran.benchmark.evaluate --task T1-concentration \\\n    --truth data/ch4-t1-test-v0.h5 \\\n    --predictions baselines/transformer_t1/predictions_t1-test.csv\n</code></pre> <ul> <li>[ ] Step 2: Run Transformer T4 baseline</li> </ul> <pre><code>python baselines/transformer_t4/train.py\n</code></pre> <ul> <li>[ ] Step 3: Run U-Net T2 baseline</li> </ul> <pre><code>python baselines/unet_t2/train.py\n</code></pre> <ul> <li>[ ] Step 4: Run TCN T5 baseline</li> </ul> <pre><code>python baselines/tcn_t5/train.py\n</code></pre> <ul> <li>[ ] Step 5: Collect all scores from hyperparams.json files</li> </ul> <p>Read the <code>hyperparams.json</code> written by each script and collect the final metrics.</p> <ul> <li>[ ] Step 6: Update leaderboard.md with actual scores</li> </ul> <p>Replace all \"\u2014\" entries with the measured values:</p> <pre><code>| Patchified Transformer | Deep | X.XX | X.X | `baselines/transformer_t1/` |\n</code></pre> <ul> <li>[ ] Step 7: Commit</li> </ul> <pre><code>git add docs/leaderboard.md baselines/*/hyperparams.json baselines/*/predictions_*.csv\ngit commit -m \"bench: fill DL baseline scores on leaderboard\n\nRan Transformer (T1/T4), U-Net (T2), TCN (T5) baselines on CPU.\nAll scores now reported on the static leaderboard.\"\n</code></pre>"},{"location":"superpowers/plans/2026-08-05-tdlas-depth/#task-3-hartmann-tran-profile-htp-line-shape-p1","title":"Task 3: Hartmann-Tran Profile (HTP) Line Shape (P1)","text":"<p>Files: - Create: <code>src/spektran/physics/lineshape_htp.py</code> - Create: <code>tests/reference_impl/ref_lineshape_htp.py</code> - Create: <code>tests/test_lineshape_htp.py</code> - Modify: <code>src/spektran/physics/hitran.py</code> (extend LineList) - Modify: <code>src/spektran/physics/absorption.py</code> (integrate HTP option) - Modify: <code>src/spektran/physics/__init__.py</code></p> <p>Implement the Hartmann-Tran Profile (HTP) \u2014 the HITRAN2016+ recommended line shape that accounts for speed-dependent broadening, speed-dependent shifting, Dicke narrowing, and their correlations. The implementation follows Ngo, Lisak, Tran &amp; Hartmann, JQSRT 129 (2013) 89-100, Algorithm 1.</p> <ul> <li>[ ] Step 1: Extend LineList with HTP parameters</li> </ul> <p>Add optional HTP fields to <code>LineList</code> in <code>hitran.py</code>:</p> <pre><code>@dataclass\nclass LineList:\n    # ... existing fields ...\n    # HTP parameters (HITRAN2020 beyond-Voigt, optional)\n    gamma_2: np.ndarray | None = None    # speed-dependent width [cm-1/atm]\n    delta_2: np.ndarray | None = None    # speed-dependent shift [cm-1/atm]\n    nu_vc: np.ndarray | None = None      # velocity-changing frequency [cm-1/atm]\n    eta: np.ndarray | None = None        # correlation parameter [-]\n</code></pre> <ul> <li>[ ] Step 2: Write HTP line-shape function</li> </ul> <p>Create <code>lineshape_htp.py</code> implementing Algorithm 1 from Ngo et al. 2013:</p> <pre><code>def htp_profile(\n    nu_cm1: np.ndarray,\n    nu0_cm1: float,\n    doppler_hwhm: float,\n    gamma_0: float,     # Lorentzian HWHM (pressure-broadened)\n    delta_0: float,     # pressure shift\n    gamma_2: float = 0.0,   # speed-dependent width\n    delta_2: float = 0.0,   # speed-dependent shift\n    nu_vc: float = 0.0,     # velocity-changing collision freq\n    eta: float = 0.0,       # correlation parameter\n) -&gt; np.ndarray:\n    \"\"\"Hartmann-Tran Profile (area-normalized, units cm).\n\n    Reduces to Voigt when gamma_2 = delta_2 = nu_vc = eta = 0.\n    \"\"\"\n</code></pre> <p>The core algorithm: compute the speed-averaged complex probability function using two evaluations of wofz, combined via the HTP formula.</p> <ul> <li>[ ] Step 3: Write independent reference implementation</li> </ul> <p>Create <code>tests/reference_impl/ref_lineshape_htp.py</code> using a completely different algorithm (direct numerical quadrature over the Maxwell-Boltzmann speed distribution, integrating speed-dependent Lorentzians).</p> <ul> <li> <p>[ ] Step 4: Write tests</p> </li> <li> <p>Degenerate limit: HTP with gamma_2=delta_2=nu_vc=eta=0 must match Voigt</p> </li> <li>Area normalization: integral of HTP must equal 1</li> <li>G3 cross-validation: main vs reference within 1e-6 relative</li> <li> <p>Known values from Table 1 of Ngo et al. 2013</p> </li> <li> <p>[ ] Step 5: Integrate HTP into absorption_coefficient</p> </li> </ul> <p>Add <code>line_profile</code> parameter to <code>absorption_coefficient</code>:</p> <pre><code>def absorption_coefficient(\n    nu_cm1, lines, mole_fraction, temperature_K, pressure_atm,\n    q_ratio=None, wing_cutoff_cm1=0.0,\n    line_profile=\"voigt\",  # \"voigt\" or \"htp\"\n):\n</code></pre> <p>When <code>line_profile=\"htp\"</code> and <code>lines</code> has HTP parameters, use <code>htp_profile</code> instead of <code>voigt_profile</code>.</p> <ul> <li>[ ] Step 6: Update fetch_lines to retrieve HTP parameters</li> </ul> <p>Extend <code>fetch_lines</code> in <code>hitran.py</code> to fetch SD_gamma_air, SD_delta_air, nu_VC, eta from HITRAN when available (columns exist in HITRAN2020 for many lines).</p> <ul> <li>[ ] Step 7: Run full test suite</li> </ul> <p>Run: <code>python -m pytest tests/ -x -q</code></p> <ul> <li>[ ] Step 8: Commit</li> </ul> <pre><code>git add src/spektran/physics/lineshape_htp.py tests/reference_impl/ref_lineshape_htp.py \\\n    tests/test_lineshape_htp.py src/spektran/physics/hitran.py \\\n    src/spektran/physics/absorption.py src/spektran/physics/__init__.py\ngit commit -m \"feat: add Hartmann-Tran Profile (HTP) line shape\n\nImplement the HITRAN2016+ recommended beyond-Voigt profile with\nspeed-dependent broadening/shifting, Dicke narrowing, and correlation.\nAlgorithm from Ngo et al., JQSRT 129 (2013) 89-100.\nExtends LineList with gamma_2/delta_2/nu_vc/eta HTP parameters.\nDual-implementation G3 cross-validation (analytical vs quadrature).\"\n</code></pre>"},{"location":"superpowers/plans/2026-08-05-tdlas-depth/#task-4-multi-species-benchmark-task-t8-p1","title":"Task 4: Multi-Species Benchmark Task T8 (P1)","text":"<p>Files: - Modify: <code>src/spektran/benchmark/tasks.py</code> - Modify: <code>src/spektran/benchmark/evaluate.py</code> - Create: <code>configs/instruments/vi-da-multispecies-13.yaml</code> - Create: <code>configs/datasets/ch4-h2o-t8-train-v0.yaml</code> - Create: <code>configs/datasets/ch4-h2o-t8-test-v0.yaml</code> - Create: <code>baselines/ridge_multispecies_t8/train.py</code></p> <p>T8: Given a noisy raw scan containing overlapping CH4 + H2O absorption, regress both concentrations simultaneously. This tests spectral deconvolution \u2014 a core challenge in real TDLAS. Uses the existing multi-species generator (interferents) but makes BOTH species prediction targets.</p> <ul> <li>[ ] Step 1: Define T8 task spec</li> </ul> <p>Add to <code>tasks.py</code>:</p> <pre><code>TASKS = TASKS + (\"T8-multispecies\",)\nTASK_SPECS[\"T8-multispecies\"] = TaskSpec(\n    task_id=\"T8-multispecies\",\n    input_signal=\"raw_scan\",\n    target=\"labels.species[*].concentration_ppm\",\n    primary_metric=\"mae\",\n    secondary_metrics=[\"mae_per_species\", \"mape\"],\n)\nSPLIT_SEEDS[(\"T8\", \"train\")] = 108_001\nSPLIT_SEEDS[(\"T8\", \"test\")] = 108_002\n</code></pre> <ul> <li>[ ] Step 2: Create multi-species instrument config</li> </ul> <p><code>vi-da-multispecies-13.yaml</code>: same as vi-da-medium-02 but with both CH4 and H2O absorption regions. The wavenumber range covers a region where both species have lines (near 6046-6048 cm\u207b\u00b9 CH4 has strong 2\u03bd3 lines and H2O has weaker lines in the same window).</p> <ul> <li>[ ] Step 3: Create dataset configs</li> </ul> <p>Train and test configs that specify both CH4 and H2O as target species (both appear in <code>labels.species</code> with their own concentration distributions).</p> <ul> <li>[ ] Step 4: Add evaluate_multispecies</li> </ul> <p>Extend <code>evaluate.py</code> with <code>evaluate_multispecies</code> that computes per-species MAE and aggregate MAE.</p> <ul> <li>[ ] Step 5: Write ridge baseline for T8</li> </ul> <p>Ridge regression with multi-output: one ridge per species. Inputs = raw_scan features, outputs = [CH4_ppm, H2O_ppm].</p> <ul> <li> <p>[ ] Step 6: Run full test suite</p> </li> <li> <p>[ ] Step 7: Commit</p> </li> </ul>"},{"location":"superpowers/plans/2026-08-05-tdlas-depth/#task-5-wms-2f1f-normalization-etalon-in-wms-p2","title":"Task 5: WMS 2f/1f Normalization + Etalon in WMS (P2)","text":"<p>Files: - Modify: <code>src/spektran/physics/wms.py</code> - Modify: <code>src/spektran/generator.py</code> (WMS branch) - Modify: <code>tests/test_wms.py</code></p> <ul> <li>[ ] Step 1: Add 2f/1f ratio signal output</li> </ul> <p>Add <code>wms_2f_1f_ratio</code> function and include <code>demod_2f_1f_ratio</code> as a signal in the WMS generator branch. This is the standard calibration-free WMS observable (Rieker et al. 2009): R(t) = |X_2f + iY_2f| / |X_1f + iY_1f|.</p> <ul> <li>[ ] Step 2: Add etalon transmission in WMS time-domain chain</li> </ul> <p>Currently etalons only affect the DA path. In the WMS branch, the transmitted intensity should also pass through etalon fringes before lock-in demodulation, since etalon fringes at frequencies near f_m produce spurious 2f signals.</p> <ul> <li> <p>[ ] Step 3: Write tests</p> </li> <li> <p>2f/1f ratio for known absorbance \u2192 verify against analytical Rieker formula</p> </li> <li> <p>Etalon + WMS: verify that etalon fringes produce non-zero 2f even with zero gas</p> </li> <li> <p>[ ] Step 4: Commit</p> </li> </ul>"},{"location":"superpowers/plans/2026-08-05-tdlas-depth/#task-6-temperature-regression-benchmark-t9-p2","title":"Task 6: Temperature Regression Benchmark T9 (P2)","text":"<p>Files: - Modify: <code>src/spektran/benchmark/tasks.py</code> - Modify: <code>src/spektran/benchmark/evaluate.py</code> - Create: <code>configs/instruments/vi-da-temp-regression-14.yaml</code> - Create: <code>configs/datasets/ch4-t9-train-v0.yaml</code> - Create: <code>configs/datasets/ch4-t9-test-v0.yaml</code> - Create: <code>baselines/ridge_temp_t9/train.py</code></p> <p>T9: From a noisy raw scan with known fixed concentration, regress gas temperature [K]. This exercises the temperature dependence of line strengths and Doppler/Lorentz widths \u2014 a fundamental TDLAS thermometry application (dual-line thermometry). The instrument config fixes concentration but samples temperature from a wide range (250-800 K).</p> <ul> <li> <p>[ ] Step 1: Define T9 task spec</p> </li> <li> <p>[ ] Step 2: Create instrument and dataset configs</p> </li> <li> <p>[ ] Step 3: Add evaluate_temperature</p> </li> <li> <p>[ ] Step 4: Write ridge baseline</p> </li> <li> <p>[ ] Step 5: Commit</p> </li> </ul>"},{"location":"superpowers/plans/2026-08-05-tdlas-depth/#task-7-laser-rin-electronic-bandwidth-p2","title":"Task 7: Laser RIN + Electronic Bandwidth (P2)","text":"<p>Files: - Create: <code>src/spektran/instrument/electronics.py</code> - Create: <code>tests/test_electronics.py</code> - Modify: <code>src/spektran/generator.py</code> - Modify: <code>src/spektran/instrument/optics.py</code> (remove dead code)</p> <ul> <li>[ ] Step 1: Implement RIN noise</li> </ul> <p>Relative Intensity Noise: flat spectral density below relaxation oscillation frequency, with a peak at f_RO. For a 2000-point scan sampled over ~10ms, RIN manifests as correlated multiplicative noise on the transmitted intensity.</p> <pre><code>def rin_noise(rng, n, rin_dBc_Hz, bandwidth_Hz, sampling_rate_Hz):\n    \"\"\"Laser relative intensity noise (multiplicative).\"\"\"\n</code></pre> <ul> <li>[ ] Step 2: Implement TIA bandwidth filter</li> </ul> <p>Low-pass Butterworth filter modeling transimpedance amplifier bandwidth:</p> <pre><code>def tia_bandwidth_filter(signal, bandwidth_Hz, sampling_rate_Hz, order=2):\n    \"\"\"Apply TIA bandwidth limit (anti-aliasing before ADC).\"\"\"\n</code></pre> <ul> <li>[ ] Step 3: Implement detector responsivity curve</li> </ul> <p>Wavelength-dependent InGaAs responsivity (roll-off near 1.7 \u03bcm cutoff):</p> <pre><code>def detector_responsivity(nu_cm1, cutoff_cm1=5882.0, peak_responsivity=1.0):\n    \"\"\"InGaAs detector responsivity vs wavenumber.\"\"\"\n</code></pre> <ul> <li>[ ] Step 4: Remove dead code from optics.py</li> </ul> <p>Delete <code>intensity_fluctuation()</code> \u2014 exported but never called.</p> <ul> <li>[ ] Step 5: Integrate into generator</li> </ul> <p>Wire RIN, TIA filter, and responsivity into the generator signal chain, gated by instrument config parameters.</p> <ul> <li>[ ] Step 6: Commit</li> </ul>"},{"location":"superpowers/plans/2026-08-05-tdlas-depth/#task-8-isotopologue-handling-extended-physics-p3","title":"Task 8: Isotopologue Handling + Extended Physics (P3)","text":"<p>Files: - Modify: <code>src/spektran/physics/hitran.py</code> - Modify: <code>src/spektran/physics/absorption.py</code> - Create: <code>tests/test_isotopologue.py</code></p> <ul> <li>[ ] Step 1: Add isotopologue abundance weighting</li> </ul> <p>HITRAN line intensities are per-molecule (already weighted by natural abundance). But when users fetch lines, they may want to filter by isotopologue or handle multiple isotopologues explicitly. Add: - <code>isotopologue_id</code> field to LineList - Natural abundance lookup table for principal isotopologues - Filter function to select specific isotopologues from a fetched LineList</p> <ul> <li>[ ] Step 2: Add line-wing cutoff documentation and defaults</li> </ul> <p>Document the <code>wing_cutoff_cm1</code> parameter added in Task 1. Set sensible defaults: 25 cm\u207b\u00b9 standard, 500 cm\u207b\u00b9 for CO2 (sub-Lorentzian far wings).</p> <ul> <li>[ ] Step 3: Commit</li> </ul>"},{"location":"superpowers/plans/2026-08-05-tdlas-depth/#task-9-sim-to-real-validation-against-literature-p3","title":"Task 9: Sim-to-Real Validation Against Literature (P3)","text":"<p>Files: - Create: <code>scripts/validate_sim_to_real.py</code> - Create: <code>tests/test_sim_to_real.py</code> - Create: <code>gates/reports/g5_sim_to_real.md</code></p> <p>Validate SPEKTRAN's TDLAS simulation against published experimental data from the literature. This does NOT require access to proprietary lab data \u2014 peer-reviewed papers publish measured spectra, noise characteristics, and WMS signal shapes that can be used as quantitative reference points.</p> <ul> <li>[ ] Step 1: Identify validation targets from literature</li> </ul> <p>Use the literature research results to identify 3-5 quantitative validation targets. Candidates: - CH4 2\u03bd3 band absorbance at 296 K, 1 atm: compare simulated line strengths   and widths against HITRAN-validated measurements - WMS 2f signal shape for CH4: compare peak-to-trough ratio against published   calibration-free WMS spectra - Noise characteristics: compare simulated Allan deviation against published   Allan deviation curves from real TDLAS instruments - Line width (Voigt FWHM) at known T/P: compare against published precision   spectroscopy measurements</p> <ul> <li>[ ] Step 2: Implement validation script</li> </ul> <p><code>scripts/validate_sim_to_real.py</code> generates synthetic spectra matching the conditions of published experiments and computes quantitative comparison metrics (residual, relative error, correlation coefficient).</p> <ul> <li>[ ] Step 3: Write validation tests</li> </ul> <p>Encode key literature values as test assertions: - Simulated HITRAN line strength at 296 K must match published values within   measurement uncertainty - Simulated Voigt FWHM at known T/P must match within 5% - Simulated WMS 2f/1f peak height ratio must be within 10% of analytical   prediction</p> <ul> <li>[ ] Step 4: Write gate report</li> </ul> <p>Create <code>gates/reports/g5_sim_to_real.md</code> documenting: - Which literature data points were compared - Quantitative agreement metrics - Known limitations and sim-to-real gap sources - DOI references for all validation targets</p> <ul> <li>[ ] Step 5: Commit</li> </ul>"},{"location":"superpowers/plans/2026-08-05-tdlas-depth/#task-10-documentation-sync-version-bump-final","title":"Task 10: Documentation Sync + Version Bump (Final)","text":"<p>Files: - Modify: <code>pyproject.toml</code>, <code>CITATION.cff</code>, <code>src/spektran/__init__.py</code> - Modify: <code>README.md</code>, <code>ROADMAP.md</code>, <code>docs/leaderboard.md</code></p> <ul> <li>[ ] Step 1: Bump version to 0.5.0</li> <li>[ ] Step 2: Update README feature list</li> <li>[ ] Step 3: Update ROADMAP with TDLAS depth phase</li> <li>[ ] Step 4: Final consistency check</li> <li>[ ] Step 5: Commit and tag</li> </ul> <pre><code>git tag -a v0.5.0 -m \"v0.5.0: TDLAS deep-dive\"\n</code></pre>"}]}